# Protocol Implementation — SUPERSEDED (decomposition reasoning layer, KB v1.7.0)
#
# This pattern-analysis file predates the decomposition reasoning layer.
# Whether to bit-bang a protocol or absorb it into a smart-pin / streamer
# edge is a spatial-computing decision; the framing and buffering that cross
# the seam are a data-flow contract. That reasoning is now DERIVED rather
# than catalog-selected. Retained as a stub so existing references keep
# resolving (Sacred Rule #7: redirect, never delete). The original content
# is preserved in git history.

pattern_id: "protocol_implementation"
category: "hardware_utilization"
status: "superseded"
superseded_by: "decomposition reasoning layer (architecture/decomposition/)"

note: >
  Superseded by the decomposition reasoning layer. "Bit-bang vs. absorb into
  the pin/streamer edge" is the spatial-computing lens; the bytes crossing
  the seam are governed by a data-flow contract (Force 2).

see_also:
  - "Spatial computing — push the protocol to the autonomous edge: p2kbArchSpatialComputing"
  - "Force 2 — data-flow contracts (the framed bytes across the seam): p2kbArchDataFlowContracts"
