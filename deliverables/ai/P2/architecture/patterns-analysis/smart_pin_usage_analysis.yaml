# Smart Pin Usage — SUPERSEDED (decomposition reasoning layer, KB v1.7.0)
#
# This pattern-analysis file predates the decomposition reasoning layer.
# Pushing work onto the 64 autonomous smart-pin edges is the spatial-
# computing lens; the pins themselves are a counted resource weighed against
# the budget. That reasoning is now DERIVED rather than catalog-selected.
# Retained as a stub so existing references keep resolving (Sacred Rule #7:
# redirect, never delete). The original content is preserved in git history.

pattern_id: "smart_pin_usage"
category: "architecture"
status: "superseded"
superseded_by: "decomposition reasoning layer (architecture/decomposition/)"

note: >
  Superseded by the decomposition reasoning layer. Moving computation out to
  the autonomous pin edge (so a cog need not babysit I/O) is the spatial-
  computing lens; the 64 smart pins are a finite resource counted in the
  budget. Concrete smart-pin modes live in the architecture/language
  reference.

see_also:
  - "Spatial computing — push computation to the autonomous pin edge: p2kbArchSpatialComputing"
  - "Counting smart pins against the resource budget: p2kbArchResourceBudget"
