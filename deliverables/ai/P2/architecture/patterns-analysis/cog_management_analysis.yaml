# Cog Management — SUPERSEDED (decomposition reasoning layer, KB v1.7.0)
#
# This pattern-analysis file predates the decomposition reasoning layer.
# The decision of how many cogs to spend and which resources each one owns
# is now DERIVED — from Force 1 (resource ownership) — rather than chosen
# from a frequency-ranked pattern catalog. Retained as a stub so existing
# references keep resolving (Sacred Rule #7: redirect, never delete). The
# original frequency-analysis content is preserved in git history.

pattern_id: "cog_management"
category: "architecture"
status: "superseded"
superseded_by: "decomposition reasoning layer (architecture/decomposition/)"

note: >
  Superseded by the decomposition reasoning layer. "Which cog owns which
  resource, and when it launches" is the output of Force 1 (resource
  ownership), not a catalog selection. Start from the method and let the
  forces produce the cog cuts.

see_also:
  - "Force 1 — resource ownership (cog lifecycle, launch order, who owns what): p2kbArchResourceOwnership"
  - "The generative method that derives the cog cuts: p2kbArchDecompositionMethod"
