# Timing Control — SUPERSEDED (decomposition reasoning layer, KB v1.7.0)
#
# This pattern-analysis file predates the decomposition reasoning layer.
# "How do parts running at different cadences stay coordinated" is now
# DERIVED — from Force 3 (rate adaptation) — rather than selected from a
# frequency-ranked catalog. Retained as a stub so existing references keep
# resolving (Sacred Rule #7: redirect, never delete). The original content
# is preserved in git history.
#
# NOTE: the retired content used the P1 idioms CNT / waitcnt; on P2 the
# system counter is read with GETCT() and waited on with WAITCT*/POLLCT*
# (logged as a correction-register finding when this file was stubbed).

pattern_id: "timing_control"
category: "hardware_utilization"
status: "superseded"
superseded_by: "decomposition reasoning layer (architecture/decomposition/)"

note: >
  Superseded by the decomposition reasoning layer. Cadence and timing
  decoupling between subsystems is the subject of Force 3 (rate adaptation);
  the concrete P2 timing primitives live in the language reference.

see_also:
  - "Force 3 — rate adaptation (decoupling cadences across a seam): p2kbArchRateAdaptation"
  - "The generative method that places the rate seams: p2kbArchDecompositionMethod"
