# Buffer Management — SUPERSEDED (decomposition reasoning layer, KB v1.7.0)
#
# This pattern-analysis file predates the decomposition reasoning layer.
# A buffer is the concrete artifact of a data-plane contract across a seam,
# and (when it absorbs a cadence mismatch) of Force 3. That reasoning is now
# DERIVED rather than catalog-selected. Retained as a stub so existing
# references keep resolving (Sacred Rule #7: redirect, never delete). The
# original content is preserved in git history.

pattern_id: "buffer_management"
category: "hardware_utilization"
status: "superseded"
superseded_by: "decomposition reasoning layer (architecture/decomposition/)"

note: >
  Superseded by the decomposition reasoning layer. A buffer realizes a
  data-flow contract on the data plane (Force 2) and, where it bridges two
  cadences, the elastic coupling of Force 3 (rate adaptation). Decide the
  contract first; the buffer follows.

see_also:
  - "Force 2 — data-flow contracts (the seam the buffer realizes): p2kbArchDataFlowContracts"
  - "Force 3 — rate adaptation (when the buffer bridges two cadences): p2kbArchRateAdaptation"
