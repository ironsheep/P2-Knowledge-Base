# State Machine — SUPERSEDED (decomposition reasoning layer, KB v1.7.0)
#
# This pattern-analysis file predates the decomposition reasoning layer.
# A state machine is a control-plane implementation idiom; which subsystem
# owns the state, and what crosses into it, is DERIVED from the method and
# the data-flow contract rather than catalog-selected. Retained as a stub so
# existing references keep resolving (Sacred Rule #7: redirect, never
# delete). The original content is preserved in git history.
#
# NOTE: the retired content used the P1 idiom CNT (state_entry_time := CNT);
# on P2 the system counter is read with GETCT() (logged as a correction-
# register finding when this file was stubbed).

pattern_id: "state_machine"
category: "hardware_utilization"
status: "superseded"
superseded_by: "decomposition reasoning layer (architecture/decomposition/)"

note: >
  Superseded by the decomposition reasoning layer. A state machine lives on
  the control plane; the decomposition decides which owner holds the state
  and what signals/data cross into it (Force 2). The idiom itself remains a
  language-level implementation choice.

see_also:
  - "The generative method (which owner holds the state): p2kbArchDecompositionMethod"
  - "Force 2 — data-flow contracts (control and event planes): p2kbArchDataFlowContracts"
