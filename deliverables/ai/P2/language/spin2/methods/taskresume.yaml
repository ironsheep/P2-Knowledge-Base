method: TASKRESUME
type: method
category: Task Operations
status: invalid_keyword

aliases:
  - "TASKRESUME"

description: |
  TASKRESUME is NOT a valid Spin2 keyword. It does not compile
  (pnut_ts: "Expected an instruction or variable").

  The real keyword for continuing a halted task is TASKCONT (introduced in
  {Spin2_v47}). This stub exists only to redirect lookups of the invalid name
  to the correct method.

redirect_to: language/spin2/methods/taskcont.yaml

see_also:
  - "TASKCONT — the correct keyword (language/spin2/methods/taskcont.yaml)"

related:
  - TASKCONT
  - TASKHALT
