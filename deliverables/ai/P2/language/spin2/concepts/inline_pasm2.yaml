concept: inline_pasm2
title: Inline PASM2 in Spin2 Methods (redirect stub)
category: hybrid_programming
description: |
  Canonical entry redirected to language/spin2/constructs/inline_pasm.yaml.
  Both keys (p2kbSpin2InlinePasm and p2kbSpin2InlinePasm2) are kept indexed
  for back-compat with existing references; this stub eliminates content
  drift between the two locations.
documentation_source: redirect_stub
documentation_level: stub
created: 2025-09-09
updated: 2026-04-29

status: |
  This file was previously a parallel description of inline PASM2. It has
  been demoted to a stub on 2026-04-29 because it duplicated and (in places)
  drifted from the canonical entry. All authoritative content now lives in
  language/spin2/constructs/inline_pasm.yaml.

key_facts_summary:
  execution_mode: |
    Inline PASM2 (ORG...END inside a PUB/PRI method) is loaded into cog RAM
    at runtime by the Spin2 interpreter (spilling the first 16 locals) and
    runs as cog-exec, NOT hub-exec.
  size_limit: "16 longs total (params + result + locals + code)"
  label_rule: "Inline labels must start with '.' or ':' (method-local only)"

canonical_source: language/spin2/constructs/inline_pasm.yaml

related:
  - language/spin2/constructs/inline_pasm.yaml
  - language/pasm2/concepts/cog_hub_execution.yaml
  - language/pasm2/concepts/execution_modes.yaml
  - language/pasm2/concepts/labels.yaml
