reconciled-against: v8
date: 2026-08-15
note: |
  Delta run over (v5, v8] — three rows (v6, v7, v8). Work-list ∩ this project's overlays =
  baseline-health, build-wrapup, document-finalize, plan-to-tasks, sprint-plan. No overlay
  deleted (all five still carry essence); none demoted from full override.

  SUBTRACTIVE. document-finalize: the claim-verification block (verify-against-primary-sources,
  four-value classification) removed — central §1 now owns it via {{DOMAIN_AUTHORITY}}; kept the
  project residue (truth-matrix-first, the MODIFIED-vs-FABRICATED distinction central's three
  values cannot express, the corrections-register routing, NEEDS-VERIFICATION tightening,
  punch-list pull-in, the multi-tree fix routing, the example-corpus identity gate).
  plan-to-tasks + sprint-plan: the PLAN_DIR restatements removed (it is a literal slot now —
  route, don't restate); the sentinel-dispatch pointer kept.

  ADDITIVE (§1a adoption actions). v6(a) DOC_AUDIT_COMMAND already set by Sprint 1. v7(a) set
  EXEC_ENV_LIMITED/CANONICAL — container authors+validates+compiles; Stephen's host holds the
  three verdicts we cannot observe here (Forge render, P2 silicon, DEBUG windows). v8(a) set
  DELIVERABLE_AUDIENCE per head — manuals human-reader, the P2KB YAML set agent-consumer, so
  the strict cite-or-omit bar now governs the KB heads automatically. v8(b) set DOMAIN_AUTHORITY
  to the ingestion tree with explicit precedence, empirical/hardware-verified first —
  deliberately NOT p2kb-mcp, which serves what this project publishes and is therefore circular
  here. v8(d) dropped MODEL_TIERS/DEFAULT_MODEL; no model-strategy overlay existed. v8(e) N/A —
  p2-dev-cycle is not in this project's workflow. v8(g) converted the three retired doc slots
  into CONFORMANCE_GUIDES rows over the Sprint-1 guide layer. v8(h) adopted central:changelog-
  voicing and central:spin2-authoring-guide rows; STYLE_GATE_COMMAND left unset = OWED.

  TWO ADOPTION ACTIONS COULD NOT COMPLETE AS WRITTEN, both recorded rather than forced:
  (1) The local changelog-style-guide.md is NOT deleted. Central's Class 3 (Published document)
  profile is an unauthored stub, so deleting the local guide would leave every manual CHANGELOG
  governed by nothing. Shared core §1-§4 adopted now; the local file retained as the Class 3
  taxonomy. Task «#216». (2) The one SPIN2-AUTHORING-GUIDE.md in the tree is a received external
  artifact inside an ingestion handoff package, not a project-owned guide — kept as part of that
  record. Nothing here is a fork.

  Also fixed a live defect the compare surfaced: BUILD_COMMAND had named
  validate-yaml-syntax.py, which returns a hollow green over the content tree — the exact trap
  the baseline-health overlay has documented for months. Now verify-yaml-format.py.

  Promotion candidates surfaced (central changes — NOT made here): the instrument's-first-run
  planning rule, the artifact-derived-scope-boundary rule, and the payoff-sentence
  fabrication-shape rule. Detector present in CLAUDE.md and verified.
