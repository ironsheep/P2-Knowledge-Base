reconciled-against: v11
date: 2026-08-29
note: |
  Delta run over (v9, v11] — two rows, 19 adoption actions (v10 a–r, v11 a).
  Work-list ∩ this project's overlays = plan-to-tasks, sprint-plan, sprint-closeout,
  sprint-retrospective, task-handoff. No overlay deleted, no rule trimmed: every rule in all
  five still passes the essence test (central-absent AND project-specific), and every anchor
  resolves against current central — checked one by one, including the two `Step 0a` anchors,
  which resolve to a bold line rather than a heading in both `plan-to-tasks` and `sprint-plan`.
  The project carries NO `task-execution`, `tracking-readiness` or `dual-agent-handoff` overlay,
  which is what makes so much of v10 `n/a` here.

  THE ONE REAL SUBTRACTION IS NOT IN AN OVERLAY. v10 retired the premise *"the arbiter's context
  can never be refreshed — one per sprint"* and (a) asks every project to correct anything
  restating it. No overlay here does. The project's AUTO-MEMORY did, verbatim, as the stated
  `Why` of the rule an agent reads every session — so it was corrected there instead, to the
  boundary/availability argument central replaced it with (renewable BETWEEN tasks, not INSIDE
  one; the clear's cost is availability, not recovery; dispatch conserves rather than clears).
  An overlay-only sweep would have reported this project clean while the wrong premise kept
  loading at every session start.

  DISPATCH_MODEL WAS ALREADY RIGHT AND WAS NOT TRACEABLE. v10(b) and v11(a) both point at the
  slot. The value — `arbiter-serial` by default, `conductor-parallel` on the ingestion head —
  is Stephen's own call (2026-08-27: *"You execute tasks by handing it off to an agent and
  waiting on the return"*, correcting a sprint that had run inline), but that provenance lived
  only in auto-memory while the conventions file argued the value from project structure. So a
  fresh agent could read the slot and not know whose decision it was, which is exactly the gap
  v11 opened. The attribution, the date, and the precedence rule (the standing "do not call the
  Agent tool unless the user requested it" does not countermand the slot — setting it IS the
  request; the authority is the arbiter's and does not travel down a dispatch chain) are now
  written into `skill-conventions.md` beside the slot. v9(l)'s "n/a, this harness HAS an Agent
  tool" is the capability answer v10 names as the wrong question; superseded below.

  ADOPTED WORK BEYOND THE LEDGER LINES. v10(r) gave `{{PUNCH_LIST_DOC}}` two new readers
  (`sprint-plan` §1 presents the open items at the scope call, `sprint-retrospective` §5 triages
  them). Here that sentinel read `TBD per manual` in `HEAD-DISPATCH-DRAFT.md` — stale for
  months: five manuals carry `workspace/<slug>/PUNCH-LIST.md` and the cross-cutting
  `engineering/document-production/PUNCH-LIST.md` states the split in its own header. Left as
  written, v10(r)'s two reads would have returned nothing and looked like an empty punch list.
  Resolved in the dispatch table and in the `punch-list-maintenance` overlay, which now also
  names an unresolved sentinel a STEP HALT, because "unresolved" and "genuinely empty" are
  indistinguishable in the output.

  §4a ABSORPTION WALK — ZERO ABSORBED, and that was verified against central's TEXT rather than
  inferred from the version rows. This project's promotion buffer is
  `feedback_skill_evolution_candidates.md` in auto-memory rather than in-tree; the same defect
  applies (an absorbed entry left OPEN is re-sent as live central work), so it was walked
  anyway. All ~35 entries stand OPEN at v11. The finding worth carrying: v10 REVISED
  `task-handoff` §6 and `plan-to-tasks` §2 — the exact two sections this buffer has standing
  proposals against — and absorbed neither. Central edited the section and left the proposal
  beside it. Re-raise at the next promotion close-out; do not assume the revision considered
  them.

  v10(m) CHECKED, NOT ASSUMED. Every worked example in the five in-scope overlays names a
  source. Spot-verified: `8167f233` and `ece5c74a` both resolve with matching subjects, and the
  «#308» quote is verbatim at its cited `file:line`. No composed or unsourced example found.

adoption:
  v8(h): superseded 2026-08-23  amended by v9(a); the 2026-08-15 refusal is now the rule
  v9(a): planned   2026-08-29  RE-OPENED and re-affirmed. «#216» still open — 3 of 4 parts done
                               at the v9 run; trimming the local changelog guide to central's
                               complement is what remains. Still gated: 4 skills cite the file
                               and the guide layer is a DOC_AUDIT_COMMAND-gated surface.
  v9(b): adopted   2026-08-23  second row recorded + punch-list item raised
  v9(c): n/a       2026-08-23  no second AGENT. The canonical side (PDF Forge, P2 silicon,
                               DEBUG-window capture) is operated by Stephen himself, not by
                               an agent holding the hardware. The trigger is the two-agent
                               split, not the domain — one agent plus a human operator is
                               the two-environment split (v7), which is already adopted.
  v9(d): n/a       2026-08-23  follows (c)
  v9(e): adopted   2026-08-23  release-yamls §6e + the release-manual overlay's published-link
                               check both cite scoped grants and name their scope
  v9(f): adopted   2026-08-23  asking-conduct block installed in CLAUDE.md
  v9(g): n/a       2026-08-23  no overlay restated how to ask
  v9(h): adopted   2026-08-23  WRITE_SCOPE + NETWORK_GRANT_ROSTER set, with forums.parallax.com
                               NAMED-EXCLUDED rather than merely absent
  v9(i): adopted   2026-08-23  complied — enforcement layer confirmed absent, gap reported, NOT
                               closed
  v9(j): adopted   2026-08-23  EXCLUSIVE_RESOURCES enumerated and CLOSED; DISPATCH_MODEL set
                               (provenance added 2026-08-29 under v11(a))
  v9(k): n/a       2026-08-23  no overlay restates the dispatch model — swept, nothing found
  v9(l): superseded 2026-08-29 SUPERSEDED BY v10(b). The recorded answer — "this harness HAS an
                               Agent tool; the model is not `inline`" — is the CAPABILITY answer
                               v10 names as the wrong question. Re-adjudicated as permission at
                               v10(b) below. Not inverted: nothing here ever wrote `inline` or a
                               do-not-dispatch note onto the slot.
  v9(m): planned   2026-08-29  RE-OPENED and re-affirmed. «#291» still open — the stop-vocabulary
                               sweep across ~8 project SKILL.md overrides. Re-wording must change
                               WHO gets woken, never what the gate detects.
  v9(n): deferred  2026-08-29  RE-OPENED, reason re-read, unchanged: the action fires "on first
                               contact" and there has still been none. WHAT WOULD CHANGE THIS:
                               the first investigation that runs out of evidence and needs an
                               instrument only Stephen can operate — ask then, once, and record
                               the answer in the overlay rather than re-asking each sprint.
  v9(o): n/a       2026-08-23  bin/check-release.py is central repo tooling; nothing to adopt
  v9(p): adopted   2026-08-23  this ledger
  v9(q): n/a       2026-08-23  follows (c)
  v9(r): n/a       2026-08-23  follows (c) — no shared filesystem between two agents to arbitrate

  v10(a): adopted  2026-08-29  No overlay restates the retired scarcity premise — swept all 24
                               overlay/override files. The AUTO-MEMORY did, as the stated `Why`
                               of the dispatch rule: *"a session cannot clear its own context, so
                               the arbiter's context is the only one that can never be refreshed."*
                               Replaced with central's boundary/availability argument, keeping
                               Stephen's quote and the measured 11-task finding.
  v10(b): adopted  2026-08-29  Re-adjudicated as PERMISSION. This project dispatches per
                               DISPATCH_MODEL without re-asking. Nothing inverted to correct — the
                               slot has never held `inline` or a do-not-dispatch note. The
                               precedence rule (setting the slot IS the request) is now written
                               into conventions so the standing directive is not read as a veto.
  v10(c): planned  2026-08-29  «#330» — rule-4 audit of this project's own skills against its four
                               declared MCP servers. Real work, deliberately not done inline:
                               ~24 skill files. Largest suspected accidental gap is
                               image-tools-mcp at ~21 tools / 0 references while diagram and
                               screenshot verification is done by eye across every manual.
  v10(d): planned  2026-08-29  «#331» — AND A QUESTION IS OPEN WITH STEPHEN. Re-verified: still
                               exactly 29 tracked artifacts, as v10 measured. Not executed inline
                               because this project is the fleet's outlier: `.claude/skills/`
                               holds three shared model documents that CLAUDE.md and four overlays
                               cite by path, plus ~3,000 lines of full SKILL.md overrides encoding
                               project process, in a repo whose premise is long-lived documented
                               knowledge. Recommendation is (a) adopt fully. Awaiting his call.
  v10(e): n/a      2026-08-29  no second side — follows v9(c)
  v10(f): n/a      2026-08-29  no second side; no baton token in this project
  v10(g): n/a      2026-08-29  no second side
  v10(h): n/a      2026-08-29  no `task-execution` overlay to trim. The harness fact was
                               RE-VERIFIED rather than inherited: no TaskCreate / TaskUpdate /
                               TaskList / TodoWrite in the arbiter's tool set or a dispatched
                               agent's, so nothing here renders a live checklist and the
                               breadcrumb carries the subtask list alone. Recorded in the
                               dispatch auto-memory.
  v10(i): n/a      2026-08-29  no second side — `task-handoff` §6's crossing question cannot fire
  v10(j): adopted  2026-08-29  Buffer walked against (v9, v11]. ZERO absorbed — verified against
                               central's text. Walk recorded at the head of the buffer with the
                               near-miss finding (v10 revised the two sections the buffer targets
                               and absorbed neither). Nothing rewritten, nothing deleted.
  v10(k): n/a      2026-08-29  no second side; no token to upgrade
  v10(l): adopted  2026-08-29  marker was already canonical (`reconciled-against:`); rewritten as
                               a FILE to canonical form this run, per §5, rather than field-set.
  v10(m): adopted  2026-08-29  Every worked example in the five in-scope overlays checked against
                               the project's own records. All name a source; two commits and one
                               quoted `file:line` spot-verified and correct. Nothing to correct or
                               relabel.
  v10(n): adopted  2026-08-29  Verified all four central execution profiles are installed
                               (`~/.claude/agents/`: task-design, task-mechanical, task-standard,
                               task-survey) AND resolvable in this session's agent roster, so
                               §1a's profile form works rather than the model-only fallback. No
                               project shadow needed: no tool-scope narrowing wanted, and the
                               ingestion head's parallelism is a DISPATCH_MODEL matter, not a
                               profile.
  v10(o): n/a      2026-08-29  records that CENTRAL is clean; the project side is v10(a)
  v10(p): planned  2026-08-29  «#330» — same audit as (c). The tag hazard central names applies
                               here with force: this board is tag-driven and already carries tags
                               no central skill mentions, so `todo_tag_add`/`todo_tag_remove`
                               would mint a second taxonomy beside a working one.
  v10(q): adopted  2026-08-29  Board audited: ZERO tasks `in_progress` and none `paused`, so
                               there was no stale-in_progress state to convert. Recorded the
                               distinction that would otherwise read as a conflict: v10(q)'s
                               `todo_pause` is a TASK HALT (blocked, still owned, has a
                               next-action) and is NOT this project's standing rule that TABLED
                               work with no next action leaves the tracker for a punch list. Both
                               memories now say so.
  v10(r): adopted  2026-08-29  The action asks that the punch list be READ at the next scope call.
                               It could not have been: `PUNCH_LIST_DOC` resolved to `TBD per
                               manual`. Resolved from the tree (per-manual
                               `workspace/<slug>/PUNCH-LIST.md` + the cross-cutting
                               `document-production/PUNCH-LIST.md`), in both the dispatch table
                               and the punch-list overlay, and an unresolved sentinel is now
                               named a step halt so an empty read cannot pass as an empty list.
                               Expect a backlog on first read, as central predicts.

  v11(a): adopted  2026-08-29  Slot re-read and its provenance established: Stephen's own
                               correction of 2026-08-27, quoted and dated, now recorded beside the
                               slot in `skill-conventions.md` together with the precedence rule.
                               Not `n/a`: the value WAS traceable to him, but only through
                               auto-memory, and the conventions file argued it from project
                               structure alone — so a fresh agent reading the slot could not tell
                               whose decision it was, which is the gap this action names.
