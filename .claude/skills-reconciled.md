reconciled-against: v9
date: 2026-08-23
note: |
  Delta run over (v8, v9] — one row (v9). Work-list ∩ this project's overlays =
  plan-to-tasks, task-handoff, baseline-health, sprint-plan, sprint-closeout,
  build-wrapup. No overlay deleted (all six still carry essence); none demoted from
  full override. This is the first run under the split marker/ledger model (v9 §1a),
  so the `adoption:` block below is new — v8 and earlier are recorded in prose in the
  previous marker and are not backfilled, per v9(p).

  THIS PROJECT IS NAMED IN THE v9 ROW. The 2026-08-15 v8 reconcile declined v8(h)'s
  "delete the local changelog guide" instruction and recorded the refusal rather than
  forcing it. v9(a) makes that the rule: where a shared guide marks the project's class
  an unauthored stub, the local file is a retained CLASS PROFILE, not a fork. Central
  also closed the false finding that decision left live — sprint-closeout §2's fork
  check would have fired at every closeout on a file correctly retained. Nothing here
  had to be undone; the second row and the punch-list item are what v9(b) asks for.

  SUBTRACTIVE. build-wrapup: the partial-coverage reasoning removed — central §3 now
  carries it ("an unauthored class means the shared core governs voice and the local
  profile governs shape, and both are read"); kept the project's coordinates (class 3 /
  mode Released, roster decides the mode) plus an EXPIRING note that the local profile
  predates central §1.5 and is silent on the mandatory theme line. plan-to-tasks: the
  currency gate's bare "STOP" re-expressed as a citation of sprint stop 4 rather than a
  locally minted stop, with the check's own failure named a step halt so a broken
  register does not wake {{USER_NAME}}.

  ADDITIVE. baseline-health gained a §2c per-task free-check list — central v9 split
  audit cadence by cost and names that list overlay material; this project had none.
  Six checks, all greps or counts, each hunting "a check that reports green because it
  never ran": the files-checked count, `pnut-ts -d` (without it the compiler ignores the
  contents of every debug() and still exits 0), index-regen before trusting crossref,
  corpus-inventory membership, guide-glob membership, and assembler pickup for a new
  chapter (Forge renders a clean PDF with the chapter missing and reports success).

  RE-ANCHORED. The sprint-plan overlay's two `§plan-authoring` anchors pointed at a
  central heading that no longer exists (central's is `## 4. Write the plan document`);
  both now cite §4. Every other anchor across the six overlays was checked and resolves.

  DEFECT FIXED IN PASSING. The sprint-plan overlay carried a DUPLICATED block — an
  earlier widening merged the old "dates and commits" section into SCOPE INPUTS but left
  its body attached to the following heading, so the pickaxe recipe, the cited-SHA
  asymmetry, and the whole «#214» certification appeared twice. Deduplicated; the recipe
  now sits once, in the section whose table references it.

  PROMOTION CANDIDATE, RE-FLAGGED (central change — NOT made here). The plan-to-tasks
  overlay's "an instrument's first run is a planning input, not just a gate" rule was
  surfaced at the v8 reconcile and central has not taken it. Central v9's plan-to-tasks
  mentions instruments only for the DOC_AUDIT_COMMAND-unset case, so the rule is still
  central-absent and still passes the generality gate. Certified here 2026-08-15 («#206»):
  the instrument's first run returned 176 findings against a planned ~100, with the excess
  in a class the hand pass barely sampled — 62 codename sites against the 5 named by hand.

  REPORTED GAP, NOT CLOSED (v9(i)). The execution-posture enforcement layer is absent:
  /etc/claude-code/managed-settings.json does not exist and no PreToolUse guard is
  installed. Posture here is DISCIPLINE, NOT ENFORCEMENT, and a session should describe
  itself that way. Probed 2026-08-23: containerized (all four signals agree) and bypassed
  — the intended combination, so the invariant holds.

adoption:
  v8(h): superseded 2026-08-23  amended by v9(a); the 2026-08-15 refusal is now the rule
  v9(a): planned   2026-08-23  «#216» — 3 of 4 parts done (second CONFORMANCE_GUIDES row,
                               build-wrapup overlay trimmed, retirement on PUNCH-LIST.md);
                               trimming the local guide to central's complement is what
                               «#216» tracks. Gated: 4 skills cite the file, and the guide
                               layer is a DOC_AUDIT_COMMAND-gated surface.
  v9(b): adopted   2026-08-23  second row recorded + punch-list item raised
  v9(c): n/a       2026-08-23  no second AGENT. The canonical side (PDF Forge, P2 silicon,
                               DEBUG-window capture) is operated by Stephen himself, not by
                               an agent holding the hardware. The trigger is the two-agent
                               split, not the domain — one agent plus a human operator is
                               the two-environment split (v7), which is already adopted.
  v9(d): n/a       2026-08-23  follows (c) — no overlay describes container-vs-host custody,
                               source-edit authority, or a run-sheet/notes split
  v9(e): adopted   2026-08-23  release-yamls §6e (push = irreversible, sprint stop 2, his go
                               EVERY time — a standing grant authorizes the destination,
                               never the act) and the release-manual overlay's published-link
                               check (read-only, standing grant) both cite scoped grants and
                               name their scope
  v9(f): adopted   2026-08-23  asking-conduct block installed in CLAUDE.md, immediately after
                               the currency check. Reinforces the standing project rule that
                               AskUserQuestion is never used here.
  v9(g): n/a       2026-08-23  no overlay restated how to ask
  v9(h): adopted   2026-08-23  WRITE_SCOPE = the working tree + the Forge exchange bind mounts
                               (the mount IS the authorization). NETWORK_GRANT_ROSTER = github.com
                               / raw.githubusercontent.com + obex.parallax.com, with
                               forums.parallax.com NAMED-EXCLUDED rather than merely absent —
                               it is treated as an attack target, never probed.
  v9(i): adopted   2026-08-23  complied — enforcement layer confirmed absent, gap reported in
                               conventions and here, NOT closed. Infrastructure work does not
                               happen in a skills session.
  v9(j): adopted   2026-08-23  EXCLUSIVE_RESOURCES enumerated and CLOSED by the four sweeps.
                               DISPATCH_MODEL = arbiter-serial by default, conductor-parallel
                               ON THE INGESTION HEAD ONLY — earned, not claimed: ingest-conductor
                               allocates every sequential ID before fan-out, stages per-agent, and
                               hands shared writes to ingest-wrap-reduce as a single-writer merge.
                               Every other head is physically serial (one Forge queue, one board,
                               one COMPLETE-OPUS-MASTER.md per manual).
  v9(k): n/a       2026-08-23  no overlay restates the dispatch model — swept, nothing found
  v9(l): n/a       2026-08-23  this harness HAS an Agent tool; the model is not `inline`
  v9(m): planned   2026-08-23  «#291» — the plan-to-tasks overlay's stop was re-expressed in this
                               run; the sweep across ~8 project SKILL.md overrides (~30 bare
                               "STOP" sites, mostly step halts wearing sprint-stop language) is
                               real work on live release gates and is tracked there. Re-wording
                               must change WHO gets woken, never what the gate detects.
  v9(n): deferred  2026-08-23  the action says "on first contact" and there has been none.
                               WHAT WOULD CHANGE THIS: the first investigation that runs out of
                               evidence and needs an instrument only Stephen can operate — ask
                               then, once, and record the answer in the overlay rather than
                               re-asking each sprint.
  v9(o): n/a       2026-08-23  bin/check-release.py is central repo tooling; nothing to adopt
  v9(p): adopted   2026-08-23  this ledger
  v9(q): n/a       2026-08-23  follows (c)
  v9(r): n/a       2026-08-23  follows (c) — no shared filesystem between two agents to arbitrate
