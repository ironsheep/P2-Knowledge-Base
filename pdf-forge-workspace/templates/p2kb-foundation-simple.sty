% P2KB Foundation Style Package - SIMPLIFIED DEBUG VERSION
% Remove complex chapter formatting to isolate brace issue

\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{p2kb-foundation-simple}[2025/08/26 P2KB Foundation Simple Debug v1.0]

% ==================== PANDOC COMPATIBILITY ====================
\makeatletter
\newcommand*{\real}[1]{#1}
\makeatother

\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\providecommand{\passthrough}[1]{#1}
\providecommand{\subtitle}[1]{\gdef\@subtitle{#1}}
\providecommand{\institute}[1]{\gdef\@institute{#1}}
\providecommand{\titlegraphic}[1]{\gdef\@titlegraphic{#1}}

% ==================== CORE PACKAGES ====================
\RequirePackage[utf8]{inputenc}
\RequirePackage[T1]{fontenc}
\RequirePackage{lmodern}
\RequirePackage{graphicx}
\RequirePackage{hyperref}

% ==================== BASIC SETUP ====================
\setlength{\parindent}{0pt}
\setlength{\parskip}{10pt}

% REMOVE ALL CUSTOM CHAPTER FORMATTING - use defaults
% This should eliminate the brace parsing issue