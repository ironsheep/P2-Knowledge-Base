% p2kb-xbyte-local.sty — XBYTE Programming Guide per-manual override layer
% Loaded AFTER p2kb-platform-content.sty so anything here overrides platform defaults.
%
% XBYTE is a "twin" manual (see methodology/presentation-platform-unification-STUDY.md):
% it uses the shared iosp-* palette and the standard platform code/reference box family
% with NO bespoke colours, environments, or block types. So this override layer is
% intentionally empty for now — it exists as the sanctioned home for any future
% XBYTE-specific skin (recolour/retitle of a catalog type) or local-tail block.
%
% To override a platform default, redefine it here (e.g. \renewtcolorbox, \definecolor).
% Add genuinely-new block types only after checking the block catalog
% (methodology/presentation-block-catalog.md) and via its promotion gate.

\ProvidesPackage{p2kb-xbyte-local}[2026/06/26 P2KB XBYTE per-manual override layer v1.0]

% (no overrides at present)

\endinput
