% P2KB IOSP (I/O and Smart Pins) User Guide Diagrams Package
% Near-empty stub - the user guide uses ASCII art and tables for diagrams,
% not TikZ. This file exists so the main template can \usepackage it without
% needing conditional logic; add TikZ macros here if future chapters need them.
%
% Author: Iron Sheep Productions, LLC
% Version: 1.0

\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{p2kb-iosp-diagrams}[2026/05/25 P2KB IOSP Diagrams Package v1.0 (stub)]

% TikZ is loaded but only a couple of libraries — keeps build time low.
% If a future chapter adds a TikZ diagram, expand library imports as needed.
\RequirePackage{tikz}
\usetikzlibrary{shapes.geometric, arrows.meta, positioning, calc}

\endinput
