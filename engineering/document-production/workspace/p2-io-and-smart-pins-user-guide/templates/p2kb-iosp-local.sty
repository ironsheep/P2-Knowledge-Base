% p2kb-iosp-local.sty — I/O & Smart Pins User Guide per-manual override layer
% Loaded AFTER p2kb-platform-content.sty so anything here overrides platform defaults.
%
% I/O & Smart Pins is a "twin" manual: the shared platform stack was originally
% DERIVED from this guide's stack, so it uses the platform code/reference box family
% and palette with NO bespoke colours, environments, or block types. This override
% layer is therefore intentionally empty — it exists as the sanctioned home for any
% future iosp-specific skin (recolour/retitle of a catalog type) or local-tail block.
%
% To override a platform default, redefine it here (e.g. \renewtcolorbox, \definecolor).
% Add genuinely-new block types only after checking the block catalog
% (methodology/presentation-block-catalog.md) and via its promotion gate.

\ProvidesPackage{p2kb-iosp-local}[2026/06/09 P2KB I/O & Smart Pins per-manual override layer v1.0]

% (no overrides at present)

\endinput
