% P2 Single-Step Debugger Manual - Main Template
% Layered stack: foundation (Pandoc compat + core) + content (code coloring).
% Chapter-based reference (top-level division = chapter).
% Version: 1.0  -  Author: Iron Sheep Productions, LLC

\documentclass[11pt,letterpaper,oneside,english]{book}

% ==================== LAYERED TEMPLATE STACK ====================
\usepackage{p2kb-ssdbg-foundation}  % Pandoc compatibility + core formatting
\usepackage{p2kb-ssdbg-content}     % Code-block coloring (Spin2 blue / PASM2 green)

% ==================== DOCUMENT METADATA ====================
\title{$title$}
\author{$author$}
\date{$date$}
\providecommand{\subtitle}[1]{\gdef\@subtitle{#1}}

% Pandoc header-includes (syntax-highlight macros, etc.)
$for(header-includes)$
$header-includes$
$endfor$

% ==================== DOCUMENT STRUCTURE ====================
\begin{document}

% Cover/title page, TOC, and copyright come from front-matter.md (the shared
% front-matter style with the Parallax-approved book artwork), prepended to the
% body — NOT hardcoded here — so this manual matches the other P2 manuals.
\thispagestyle{empty}
\enlargethispage{2cm}

% Main content from Pandoc (front matter first, then chapters)
$body$

\end{document}
$for(include-after)$
$include-after$
$endfor$
