# YAML Head — P2KB Dashboard

> The **YAML knowledge-base head**'s standing-state board: what shipped recently
> (release ledger) and what hardware the KB serves (inventory). The served data
> lives in `deliverables/ai/P2/`; the open to-do list is
> [`P2KB-CORRECTION-FINDINGS.md`](P2KB-CORRECTION-FINDINGS.md). This board is an
> engineering doc — the YAML never links here.
>
> **Green** = `validate-yaml-syntax.py` + `validate-crossref-keys.py` +
> `validate-dod-release.py` all pass. **Release** = two-commit Path B (content →
> regenerate index → tag the index commit). Versions are git tags
> (`--sort=version:refname`).

## Release ledger (most recent first)

| Version | Date | What & why |
|---------|------|------------|
| **v1.16.4** | 2026-08-16 | **The qualifier we dropped, and the manual defect it caused — F-273.** `_RET_` (condition `%0000`) is **"execute the instruction, then return ONLY IF it did not branch"** — stated by **two independent Parallax primary sources** that have been in the ingestion tree all along: the *P2 Assembly Language Manual* (2022-11-01) condition table **p.68** (*"always; execute instruction then return if no branch; no context restore"*, printed twice) and the *P2 Instructions v35 Rev B/C Silicon* spreadsheet **row 410** (*"Execute `<inst>` always and return if no branch. If `<inst>` is not branching then return by popping stack[19:0] into PC"*, stack-effect column `Pop`). **Our KB carried it as an unconditional "Always + Return"; the phrase "if no branch" appeared ZERO times in `deliverables/ai/P2/`.** `CALL` branches, so `_RET_ CALL` never returns — **by specification, not by malfunction** — and an author reading our entry writes `_RET_ CALL #set_nz` and is right to. That is exactly what the XBYTE Guide did, and it reached community review because every layer below it agreed with itself. Applied: `conditional_execution.yaml` (the authoritative condition table — `%0000` row corrected, new **`ret_prefix_rule:`** block carrying semantics · the branch case · the correct `CALL`+`RET` form · why the failure is silent and layout-dependent · what the prefix *does* work on · both citations, plus a `common_mistakes` entry and `_RET_` query tags); `PASM2-ENCODING-REFERENCE.md` (cell + sourced callout); `call.yaml` (qualified to **NON-BRANCHING** + `ret_prefix_caveat:`); `ret.yaml` (**had no mention of the prefix at all** — now carries `ret_prefix_form:`, aliases and cross-refs). **The second lesson is the audit artifact:** `manual_category_alignment_check.yaml` certified this category *"PERFECT - Complete match"* while calling `_RET_` a *suffix* — it compared the **list of condition names**, which was complete, and never compared their **semantics**. **Name coverage is not semantic coverage**, and a check that compares only names must say so rather than certify the category. Evidence layer brought into agreement in the same pass: **EF-058** reframed from a hardware discovery to corroboration of documented behaviour, with its false *"dispatch does not resume"* clause corrected (the rig's own trail shows all four bytecodes dispatched plus one **spurious** handler whose `ret` restored dispatch — the failure mode is **silent EXTRA execution**, not a hang); **F-256** recast as a documentation finding needing **no further rig run**, for generality or confirmation; the campaign's authoring hold on §15.3 lifted. Validators unchanged: 1129/0 · all crossrefs · DoD pass. **Manual half APPLIED 2026-08-16 («#227»)** — the XBYTE Guide's §15.3 now defines `set_nz` with its calling convention and carries a hardware callout stating that `_RET_ CALL` never returns; its structural `CALL`+`RET` fix already stood. Ships in XBYTE v1.0.2. |
| **v1.16.3** | 2026-08-16 | **The early YAML pass of the 2026-08 manual-corrections sprint — the KB shipped BEFORE the documents that cite it.** Eight corrections, every one traced to an EF ledger entry or a primary source, and every one verified absent from `deliverables/ai/P2/` before scoping. **The ordering is the point:** Streamer §17.1's protocol and Assembly ch.5's CORDIC rule are *the same facts* as the entries below, so they now derive from a published KB instead of being written in parallel with it — the standing fix for the **F-211/F-245** recurrence, where a correction landed in the YAML and never reached the manuals, twice. Drained **F-264** (`spin2/methods/wrpin.yaml` — `tt_field` gave one context-free `%TT` meaning as *the* meaning, and `p_oe_required_for` named DAC without qualification; the highest-severity class we carry, guidance that applied in a legitimate context **breaks working code** — EF-055 measured a level-driven `P_DAC_124R_3V` pin drop from 1,305 of 2,000 samples to **25** when `P_CHANNEL` was added. Now states the four contexts with the smart-pin-off `DAC_MODE` row spelled out. Scoping check: `smart_pins.yaml:263-265`'s `which_modes_need_p_oe` was re-read and is **already correct** — its DAC entry names `%00001-%00011`, the smart-pin modes — left untouched). **G-004** (the USB smart pin's shipped `open_questions:` block deleted — a hedge in an agent-consumed deliverable, and the **only** one in the tree; replaced by a datasheet routing line. Nothing was ever Chip-gated: the content is electrical characteristics, out of KB scope). **F-265** (`streamer/dds-goertzel.yaml` — the ADC pin is **RAW**, `p2-documentation.txt:3997-3998` verbatim, and the tension with the STREAMER intro's looser *"smart pins configured as ADC's"* is now **named in-file** so it cannot re-mislead. **The defect was wider than the finding:** the file's own `usage_pattern` was the very thing the rule forbids and carried three more — `drvl` on the ADC pin, `xcont dds_cmd, #0` (S=0 sums nothing, when `S[15:12]` is mandatory), and a frequency setup missing both `SETXFRQ` and the 2³¹ halving its own `frequency:` block documents four lines above. All repaired against the Silicon Doc's worked program at `:4170-4185` / `:4289-4305`). **F-260** (`pasm2/getxacc.yaml` — **the mechanism is now SOURCED, not inferred**, which is what changed since the bench: `p2-documentation.txt:4096-4099` states the accumulators are *captured into holding registers and cleared*, and that *subsequent GETXACC instructions return the same values until a new streamer command executes* — corroborated by the Silicon Doc demo's own comment *"get prior Goertzel acc's"*. That single rule accounts for every EF-056 observation without over-reaching, so the retired "never zeroed" framing stays retired and the `XINIT`-vs-`XCONT` question it raised is **dissolved rather than asserted**. Untraced `documentation_source: original` replaced with the citation). **F-263** (`architecture/cordic.yaml` — hub I/O out of both loops, stated as the **tested shape** with the arm table and an explicit `scope:` note that the measurement shows *where* results are lost, not *why*; `ops_in_flight_per_cog` upgraded derived → empirically supported. EF-053). **F-266** (`streamer/overview.yaml` — the `DEBUG_COGS` default surfaced where a streamer author meets it; the limitation already existed in `debug_interrupt.yaml` and reached nobody. EF-057). **F-259** (the composition rule **stated once**, in `smart_pins.yaml` `configuration_format.composition_rule`, with `wrpin.yaml` pointing at it rather than restating). **F-268 filed and applied** from EF-060, which had carried no F-number: `##hubsymbol` in a Spin2 object's `DAT` resolves against `$400` (measured 5,288 bytes off) — into `spin2-pasm2-integration.yaml`, with a pointer from `special-symbols/at.yaml`, which documented the Spin2 side of that boundary and had no route to the PASM side. Findability also: `streamer/overview.yaml`'s `command_format` now says outright that its map is the common case and that mode fields are positional — composing with `+` against bits a mode already defines selects a **different mode**, silently (EF-059). Validators unchanged from the sprint entry baseline. **Manual halves APPLIED 2026-08-16** — F-259/260/263/266 are all `DONE`: «#220» «#221» «#228» «#236», plus F-266's Streamer §14.5, which was found missing in a coverage audit after «#221» closed without it. Ship in Streamer v1.0.9, Assembly v3.1.6, P2AN002 v1.0.3. **Also folded in (this bump was not yet tagged, so no double-bump): F-269 — the reversal of F-211.** «#219» instructed grounding against the domain authority rather than F-211's summary text; doing so returned the OPPOSITE of what the task asked us to write, so the task was stopped rather than executed. **There are TWO groupings and F-211 wrote the wrong one into the silicon file.** SILICON = **16 domains of FOUR** (Silicon Doc v35 Part 1 p.9 pinout: sixteen VIO pins `VIO_0_3`…`VIO_60_63` + sixteen internal `GIO_0_3`…`GIO_60_63`; rails repeat `P(n) P(n+1) VIO_n_(n+3) P(n+2) P(n+3) VDD` with each VIO **centred in its own four**; package closes at exactly 100 = 64 I/O + 16 VIO + 16 VDD + TEST/RESN/XI/XO — eight VIO pins would leave eight unaccounted; plus the datasheet's *"powered in groups of 4 via VIO pins"*). BOARD = **8 LDO groups of EIGHT** (`edge-module-breakout-compatibility-matrix.md:69-83` — eight independent 3.3 V LDOs, `V00`→P0-7 … `V56`→P56-63, 300 mA each). **One Edge LDO feeds TWO silicon VIO pins**, which is why the headers show 8 while the chip has 16 — Stephen raised exactly this on challenge, and it is what closed the finding rather than what contradicted it. F-211's three evidences: our own `VERIFICATION-OPPORTUNITIES.md:57` note cited back to itself; the Edge **board** header spec imported into the chip's file; and "the Silicon Doc is silent", inferred from the text table without opening the figure. It also **deleted a true datasheet citation as "fabricated"**. **Remediated as an ENRICHMENT, not a revert** — restoring "4" alone leaves the KB one breakout-header challenge away from flipping back, so `pin-power-domains.yaml` now carries both layers named and separated (`silicon_power_grouping` / `board_power_grouping`), with `why_four_and_not_eight` stating that adjacent silicon groups share an LDO *net* on an Edge but not the same package pin or bond wire — so **"stay within four" holds on every board** and "within eight" is an Edge-only relaxation. The three ADC files and the P2AN001 companion now **point at** that page instead of carrying their own copy of the number. `hardware/edge-*.yaml`'s "one LDO per 8-pin group" is **board-level and correct — deliberately not swept**. IOSP `chapter-16-adc.md` is **correct as shipped, citation included**, so F-261 is `RESOLVED-INVALID` and IOSP needs no repair for it. **Process lesson:** a class-wide sweep amplifies whatever fact it starts with, so grounding must be *stronger* before a sweep than before a single-site fix — and a **board** document can never establish a **silicon** fact. **Also folded in (same untagged bump): F-270** — found while draining F-269's own blast-radius list, which had named two seeding artifacts that were still unrepaired while the finding read `CONFIRMED`. `application-notes/p2an001-single-pin-instrumentation-adc.yaml:105` told readers to keep the SINC2 period a power of two; that is the **SAMPLING**-mode restriction, and the note's builds run **FILTERING**, where a `WYPIN` after the `WXPIN` sets any period up to **11,585** clocks (`p2-documentation.txt:8421-8425`: the override applies to every mode but SINC2 Sampling). **P2AN001 v1.0.2 corrected this in the document five weeks ago and never propagated it to the companion**, so the two halves of one released deliverable contradicted each other. Sweep done — **single-site**; IOSP `chapter-16-adc.md:601` scopes it correctly to sampling mode and `getxacc.yaml:56`'s power-of-two guidance is the unrelated Goertzel iteration-count constraint. **The durable half is a process rule, not a fact: an app-note correction is not complete until its YAML companion carries it** — the companion ships under the same version and is half the deliverable. |
| v1.16.2 | 2026-08-11 | **The missing-`P_OE` class, found on a reader's bench and swept out of the KB's own examples.** Drained **F-245/246/247** (5 config calls across `pasm2/wrpin.yaml`, `spin2/methods/pinstart.yaml`, `pasm2/concepts/streamer_smartpin_control.yaml` configured an output mode without `P_OE` — the smart pin runs, the pin driver stays disabled, and **both forms assemble clean**). Origin: a reader reported DeSilva Ch.1's fade experiment dead on real silicon; the same defect was live here, plausibly the copy source. Two files were **self-contradicting** — `wrpin.yaml` carries `p_oe_required_for` in its own `tt_field` and shipped a counterexample; `streamer_smartpin_control.yaml` documents `P_OE=$40` four lines above the example that omitted it. The SPI-clock site (`pinstart.yaml:96`, attributed `source: P2-FLASH-FS`) was held back until **five** independent sources agreed — including our own hardware-verified 2026-06 smart-pin tests — because we do not call a defect in shipping code from a YAML excerpt. **F-249** closed on a hardware observation (Stephen, both Edge modules): the LED DIP switch is labeled **ON/OFF**, ON powers the LEDs — the prior wording named the function but never the position, which is exactly what cost bench time. **F-248** closed from a source that was in the repo all along: the #64000 guide states the 8 buffered LEDs are on **P56-P63**. **F-250 filed (→ ingestion, important):** that guide's font encoding drops **every digit** under `pdftotext` — the ingested narrative has digits on 91 of 1315 lines vs 368 after forced OCR — so no pin-number grep could ever hit it, the gap was mistaken for source silence, and **every numeric fact derived from that extraction is suspect**. Proposed general gate: digit-density sanity check on hardware-doc ingestion. **F-251 filed (→ manual):** the DeSilva aside shipped hours earlier explains the touch-a-pin glow through the LED; on these **buffered** boards the floating buffer input drives it — conclusion right, mechanism wrong. |
| v1.16.1 | 2026-08-09 | **PNut-TS v1.55.3 revalidation of the preprocessor set — the sweep that found the v1.16.0 pass had inherited a bad upstream input.** Drained **F-234…F-244** + **ENH-05**, and closed **ENH-02/ENH-03** as verified-already-applied (their substance shipped in v1.16.0; the statuses had simply never flipped, and ENH-03's `compiler_portability` block grew 2 → 6 divergence rows). Trigger: Stephen supplied the v1.55.3 arm64 build (devcontainer `Dockerfile` bumped `…015502.zip` → `…015503.zip`, which had been pinned to a zip no longer in the tree). v1.55.3 answered all three items v1.16.0 had referred back to him — the `__PNUTTS__` doc typo, the missing `__VERSION__`, and the unguarded `#UNDEF` of a built-in — **and** changed `#DEFINE`/`#UNDEF` semantics **and** shipped a line-by-line re-verified `Preprocessor.md`, which is what exposed the rest. **Provenance is the lesson:** the false claims came into the KB *verbatim* from an upstream input doc, `external-inputs/pnut_ts_facts/Preprocessor-Usage-Guide.md` — §"-U (Undefine Symbol)" and `#pragma exportdef HARDWARE_REV 2`. We copied it faithfully; it was wrong. A supersession banner now sits on that guide so a later ingestion pass cannot re-infect the KB. **F-234** — `-U` was documented as "prevents a symbol from being defined"; probed, a source `#DEFINE` survives `-U` completely, and `-U`'s *only* effect is blocking a `#PRAGMA EXPORTDEF` (verified both ways through a real parent/child object pair). The old "-U is processed before -D" note described a mechanism that does not exist. **F-235** — `-D symbol=value` appeared in 3 files; there is **no such form in either compiler**, and PNut-TS accepts it while defining **nothing, with no diagnostic** — a reader following us got a silently dead symbol and a silently skipped `#IFDEF`. **F-241 (severity raised on test, F-231 class)** — the shipped `pragma-exportdef` example used `#IF`/`#ELSEIF`, which do not exist: the body compiles **unconditionally** and the build then fails at the *matching `#ENDIF`*, lines away from the cause; a reader who "fixes" it by deleting the stray `#ENDIF` gets silently ungated code. **F-240** — `#PRAGMA EXPORTDEF` exports **presence, not value** (probed: the child's `#IFDEF` sees it, `byte "SYM"` in the child stays literal), retiring an invented 3-level precedence table and 5 value-passing examples. **F-236** — `-D` symbols **can** be `#UNDEF`'d (the `__*__` built-ins are what's protected). **F-237** — "symbols have no value" is **PNut-true, PNut-TS-false**: a divergence, not an error. **F-238** — "Defined as 1/2" implied an observable value; presence-only symbols never substitute and there is no `#IF` to observe them with. **F-242** — the symbol-name limit is **32**, not 31 (v55 line 166); wrong for both compilers, not a divergence. **F-243** — the 16-symbol cap is PNut's (attributed, F-230 treatment). **F-244** — `error.yaml`/`warn.yaml` parameter descriptions still contradicted their own F-232-corrected notes. **Method note:** every claim was settled by compiling a probe and reading the `-i` intermediate — including catching that our own new example used `REV`, a PASM2 mnemonic, as a CON name. PNut's side came from the ingested v55 v48 release note, not from asking Stephen. **Two upstream items for PNut-TS:** `-D SYM=value` should diagnose rather than silently drop the definition, and `#if`/`#elseif` should be caught at the `#if`. |
| v1.16.0 | 2026-08-08 | **Spin2 preprocessor corrections + coverage, from the PNut-TS v1.55.2 update request — and the licensing-statement removal.** Drained **F-229…F-233** and **ENH-02/03/04** across the 14 `language/spin2/preprocessor/` YAMLs. **F-229** — 18 occurrences in 9 files claimed symbols are case-**sensitive**; ground truth is `spin2-v55-text.txt:165` ("Source code is case-insensitive") **and our own `fundamentals/case-sensitivity.yaml`**, which the preprocessor set had been contradicting since before PNut-TS ever tested it. Blast radius ~5× what the request stated (it named 3 places); `external-symbols.yaml` (the `-D` side) swept in under the class-wide rule. **F-230** — the 8-level nesting cap was stated as a language rule; `spin2-v55-text.txt:40` carries it inside **PNut's own v48 release note**, so it is PNut's, and it was *attributed, not deleted* (7 occurrences, 5 files). **F-231 — severity RAISED on test:** our shipped `contradictory_conditions` "correct" example used a defensive `#UNDEF`; the request called it a warning, but on the then-installed **v1.55.0** the preprocessor fails to consume a `#UNDEF` of a never-defined symbol, leaves it in the preprocessed output (read back via `-i`), and the parser rejects it — **no binary produced.** We were recommending a build-breaking pattern. **F-232/F-233** were *not in the request* — surfaced by reading the files: the `#ERROR`/`#WARN` "must be quoted" claim (quotes are optional), and four directives advertised as PNut-available when `Preprocessor.md:257` states they are PNut-TS extensions (v55 contains **zero** occurrences of them). **Toolchain upgraded mid-work** — Stephen supplied the v1.55.2 arm64 build, devcontainer now installs `…015502.zip`, and every ENH-02 claim was re-tested first-hand rather than taken on authority (warning text matches character-for-character; `#ERROR` verified firing in a taken branch and silent in an untaken one). **ENH-04** — the KB documented **1 of 8** predefined symbols; probing each one caught two errors in the compiler's *own* `Preprocessor.md`: `__PNUTTS__` is a typo for **`__PNUT_TS__`** (Stephen called it; copying the doc would have made every PNut-TS-only guard silently evaluate false) and `__VERSION__` is defined under no spelling (omitted, pending the v1.55.3 fix). Also completes the **licensing-statement removal** — the 6 surviving third-party license terms (app-note + 2 hardware) are gone; the shipped KB now states no licensing terms while `p2-source-file-organization-standard.yaml` keeps its authoring template. Stale `pending KB publish` markers on **F-206/207/208/211** corrected — all four actually shipped in v1.15.0. |
| v1.15.0 | 2026-07-27 | **DEBUG display window naming + the `PC_KEY`/`PC_MOUSE` escape backtick — the KB half of Debug Window Manual v1.1.1.** Drained **F-227** and **F-228**, both surfaced by Stephen running all 34 of the manual's example programs against PNut v55 on real silicon (4 failed). **F-227** — `debug-commands/pc_key.yaml` + `pc_mouse.yaml` and the `PC_KEY`/`PC_MOUSE` entries in all 7 window YAMLs that carry them showed the command **un-backticked**. Inside a display message that is not a command at all: the compiler emits the literal characters (proven by reading the emitted display strings out of two `pnut-ts -d` binaries), the pointer is never written, and a generated control loop polls forever responding to nothing — with **no compile error**. The KB was teaching the broken form, as was the manual. Both YAMLs gained a `usage_rules` entry naming the silent failure; both worked examples corrected. **F-228** — `statements/debug.yaml` gained `window_name_rules`, new content with no prior KB coverage: a display **named after a display keyword** is never declared (isolated on silicon by a six-way probe — five SCOPE creates opened, the one named `Trace` did not, from byte-identical create lines). Rules are from an agent study of the **PNut v55 `p2com.asm`** source commissioned by Stephen (`debug_symbols` @19335, `check_word_chr` @8888, `symbol_size_limit`=30), replicated to `manuals/p2-debug-window-manual/REF/DEBUG-WINDOW-NAME-RULES.md`: legal iff leading letter/`_`, not one of the **103** reserved display words (enumerated in-YAML for code generation: 9 types + 11 colors incl. **both GREY and GRAY** + 19 color modes + 12 packed modes + 52 directives), not a currently-open name, case-insensitive, 30-char truncation. Scope correction worth carrying: **Spin2's own reserved words do not collide** — the compiler emits the display name as raw text (a PLOT named `Field` is hardware-run and fine). Manual-side twin = Debug Window Manual **v1.1.1** (released 2026-07-27, ch02 + ch12 + ch15 + App A). |
| v1.14.5 | 2026-07-16 | **XBYTE mode-operand bit 1 defined + complementary-PWM dead-band corrected.** Drained **F-223** (`architecture/xbyte_engine.yaml`): mode-operand bit 1 is the index-form selector (0=low-bits, 1=high-bits index; ignored in 256-mode) — documentary from the full Silicon Doc v35 mode table AND Chip-confirmed; replaced `x_bit_undocumented` with `index_form_bit` and added `alt_pattern`/`alt_index_calc` to the 128/64/32/16 entries (retires Chip Q7). Drained **F-225** (`architecture/smart_pin_patterns.yaml` motor_pwm_with_deadtime + `language/pasm2/concepts/streamer_smartpin_control.yaml` pwm_sawtooth): both had framed dead-time as a single-pin PWM feature — corrected to the two-Smart-Pin truth (high=true / low=inverted `P_INVERT_OUTPUT` + software dead-gap), Chip-confirmed + grounded in `isp_bldc_motor.spin2`. Also **F-226** (DAC ENOB unmeasured → no number). Surfaced by the F-223 "what else did we miss?" same-class sweep; manual-side twin = IOSP §9.1 dead-band note (ships with the IOSP patch). |
| v1.14.4 | 2026-07-15 | **Scope-mode filter DC-range caveat (%11010) + XBYTE-engine example fixes.** Two-file KB delta. (1) `smart-pin-11010-adc-scope-trigger.yaml`: Silicon-Doc re-scan (RA-31 / Q2, prompted by a scope-filter figure recollection) resolved a would-be Chip question from the KB's own authority — scope samples normalize to 8 bits but the effective DC dynamic range is only ~5–6 bits per filter length (Silicon Doc `p2-documentation.txt:8778`); the `X[1:0]` filter selector was already present (v1.13.3). (2) `architecture/xbyte_engine.yaml`: drained **F-220–223** (surfaced during XBYTE Guide technique-mining, ground-truthed against nine implementations) — all three `programming_examples` now compile + dispatch (LUT entry packs SKIPF pattern in [31:10]/address in [9:0], not `@`-hub `<<23`; interpreter arms via `PUSH #$1FF` + `_RET_ SETQ #mode`; a non-assembling `_RET_ NOP` removed) and mode-operand bit 1 (`x`) marked undocumented pending Chip (Silicon-Doc-upstream gap, task #54 Q7). Manual-side twins: IOSP ch16 §16.5 + App F (v1.0.6, prepared); XBYTE Guide (held pre-release). |
| v1.14.3 | 2026-07-14 | **DEBUG-display sweep — the KB half of the coordinated Debug release.** Drained **F-212 + its 2026-07-14 addendum** (13 files: the 9 window YAMLs + `pc_mouse`, `statements/debug`, `debug-formatters-complete`, `constants/special-configuration-symbols`) and the YAML half of **F-216**. Unblocks the Debug Window Manual's YAML-HEAD drain gate (manual v1.1.0 ships alongside). **Inversions killed** — the class that silently mis-teaches a code-generating agent: `fft` MAG recorded as a right-shift *divisor* (it is a GAIN ×2^n — the YAML said MAG attenuates when it amplifies); `spectro` axes; `midi` velocity-0-note-on "does NOT release" (false limitation) + velocity-sets-*color* (it sets fill HEIGHT); `pc_mouse` `coordinate_basis_per_window` documented the on-screen READOUT as the wire value (5 of 9 windows return RAW CLIENT PIXELS — any agent-built hit-test was silently wrong); `term` listed a `LIME` keyword that does not exist. **Unsourced claim removed:** `scope`/`scope_xy` "(12 modes; default LONGS_1BIT)" — there is no packing default (unpacked; silicon-confirmed). **Hardware verdicts folded in:** 🏆 EF-061 (PLOT `CARTESIAN` bottom-left/Y-UP — the single most important PLOT fact, previously absent from the KB entirely; absorbed at this release under the id EF-020, renumbered 2026-08-16 per F-267), EF-042 (SPARSE + the DOTSIZE≥4 gate), EF-043 (no-POS placement is TOOL-DEPENDENT — swept across **all nine** windows, where one tool's auto-layout had been recorded as a P2 fact), EF-048 (OPACITY 256→0), EF-052 (runtime `RATE -1` freezes BITMAP). Plus `CLOSE` semantics + the real TITLE default caption across all nine; `logic` TRIGGER → XOR form + the count-before-color rule; `DEBUG_MASK`'s stale `{Spin2_v46}` gate (the same file twice said none is required); `DLY` last + releases LOCK[15]. |
| v1.14.2 | 2026-07-06 | **Cooperative-tasking + pattern accuracy; ADC ENOB terminology.** Drained F-196–F-201. F-196: removed the fabricated `taskwait.yaml`/`taskresume.yaml` (policy call — no invalid-keyword stubs; fake names resolve to nothing, recover via `p2kb_find task*`), redirected inbound refs to valid siblings (full paths), added a positive wait-idiom note to `tasknext`. F-197: `taskspin` expression-return (task#/-1). F-198: cog-local task IDs (top-level = that cog's task 0) — **🏆 EF-023** hardware-verified, `taskid`/`taskhlt`. F-199: `spin2_shared_memory` → P2 `LOCKTRY`/`LOCKREL` (compile-verified). F-200: `spin2_event_dispatcher` SPSC scope. F-201: `smart-pin-11001-adc-external-clock` SINC "ENOB" → nominal bits (Silicon-Doc misuse, Chip-conceded). |
| v1.14.1 | 2026-07-05 | **P_STATE_TICKS example single-read cleanup.** Drained F-195 — `smart-pin-10000-time-a-states.yaml` PWM example read `rdpin()` up to 3× per loop iteration (test-read + branch re-read, risking a cross-transition mismatch on fast signals); collapsed to one `duration := rdpin()` capture (bit 31=C state, bits[30:0]=ticks), compile-verified `pnut-ts -d` v1.55. Surfaced by an IOSP reader question that also produced F-194 (`RESOLVED-INVALID`: Spin2 `RDPIN()` `Zval[31]=C` is correct per v55 L543-4 — the reader hit a FlexSpin/PNut discrepancy, not a KB defect). Manual-side twin: IOSP Ch13 `analyze_pwm`/`pwm_analyzer` → v1.0.2. |
| v1.14.0 | 2026-07-04 | **Hardware-verified smart-pin/event-wait accuracy + AppNote companions served.** Drained F-192 (🏆 EF-017, test70): concurrent single-signal counter cells (%10101/10110/10111) hang on A-only routing — the period-aligned window closes on a B-rise — so `smart-pin-10110/10111` neighbour cells now route `P_MINUS*_A \| P_MINUS*_B`; supersedes F-187. F-193 (🏆 EF-020, test74): the `SETQ`+`WAITSEx` single-instruction event-OR-timeout wait + the no-SETQ free-clear corner case → new `no_setq_behavior` field on all 15 timeout-family instructions (`waitse1-4, waitct1-3, waitatn, waitfbw, waitint, waitpat, waitxfi, waitxmt, waitxrl, waitxro`; WAITXMT was a folded-block-scalar straggler). F-186 straggler (`10101` duty → MULDIV64); G-005 closed from EF-016 (async-TX first-byte NOT-OBSERVED, gotcha reframed). AppNote companions P2AN001–004 now MCP-served via an `application-notes`→`AppNote` index handler. Also HW-proven this wave: F-165 (signed C-flag true-sign, EF-018), F-139 (NCO phase-lock + sync-serial gapless under reordered init, EF-019). Manual-side twins ship as phase-2 patches: IOSP ch05 (event-wait timeout pattern) + ch15 (concurrent A+B); PASM2 `instructions-w.md` (SETQ + no-SETQ across the WAIT family). |
| v1.13.3 | 2026-07-03 | **ADC scope-trigger register correction (%11010).** Drained F-191 — `smart-pin-11010-adc-scope-trigger.yaml` documented the trigger level in Y; the Silicon Doc (`p2-documentation.txt:8781-8810`) puts it in X (WXPIN: X[15:10]=B, X[7:2]=A, X[1:0]=filter; A/B relationship sets arm-then-trigger; RDPIN returns the 8-bit sample, C=armed). Reworked x/y/z registers, timing, in_flag, both code_examples (WYPIN-trigger → WXPIN A/B/filter), notes; added `related:` SETSCP/GETSCP. An inverted finding from the IOSP release-gate audit (the manual was right on the register). **Clears the IOSP v1.0.0 drain gate.** Manual-side twin: IOSP App-F %11010 bit-fields corrected (ships with the IOSP release). |
| v1.13.2 | 2026-07-02 | **Forum-thread Y-row reconciliation (Chip Gracey threads 170882/176065).** Drained F-188 + F-189 — the 32-bit `(a*clkfreq)/1_000_000` overflow class → `MULDIV64` — across `smart-pin-00111-nco-duty`, `spin2/patterns/applications/motor_controller`, and `spin2/concepts/timing_operations` (µs→cycles was taught as the "better" idiom while overflowing past ~21 µs). F-190: SINC2 Goertzel constant-iteration silicon limitation added to `pasm2/getxacc.yaml` (source = Chip's designer report in 176065; the released Silicon Doc still lacks the 2024-12-16 note). Y2/Y4/Y5 verified already-correct (`rep` interrupts-stalled; `qmul`/`qdiv` unsigned; `setpiv` D[7:0] / `blnpix` / `rgbsqz` / `rgbexp`) — no change; Y3 8-clock hub-window spacing already documented. Manual-side twins ship separately: IOSP (Wave-1) + Streamer §10.4 (Wave-3). |
| v1.13.1 | 2026-07-01 | **Constant-name integrity + coverage (systematic `P_*`/`X_*` audit).** Legality-arbiter = `pnut-ts` v1.55, enumeration = v55. Drained the fictitious-constant class across smart-pin/streamer/pin-config examples: F-174 (USB `P_OE`), F-175 (broken `PINREAD & $8000_0000` IN-wait, 6 sites), F-176 (`P_B_A_INPUT` invented — 21 occ, the manual had already caught it), F-177 (`P_QUADRATURE_A`), F-178 (`P_TRANSITION_OUTPUT`), F-179 (`io_pin_timing` P1-era fragments rewritten), F-180 (`P_DAC_DITHER`→`_PWM`), F-181 (`P_HIGH_1M5` fabricated), F-184 (`X_DACS_ON`), F-185 (`X_DDS_GOERTZEL`→`_SINC1/2`). F-182: added the 32 missing legal v55 `P_*` pin-config constants to the symbols catalog (values compiler-certified). Corpus now contains ONLY legal compiler symbols (Y-legal ⊆ v55). |
| v1.13.0 | 2026-06-30 | **Decomposition layer expansion + object-shaping + CORDIC/ADC fixes.** Two external arch contributions ingested (multibus object-shaping; magTile decomposition memo) → 5 new entries: `worked-derivation-streaming-pipeline` + `shared-bus-broker` + `shared-bus-replication` + `object-image-dedup` + `pin-power-domains`. Decomposition enrichments: observability eval lens, fan-out-publication contract (B2) + frame-pool sizing (B7), decimation-placement (B3), one-forcing-sentence-per-cog (B6), as-built-audit practice (B9), field-corroboration (B8), bit-bang smell-with-escape (B10), c4↔observability distinction, breadth_gap_note closed. §3 dedup reproduced on our pnut-ts v1.55. Drained F-171 (CORDIC 7-8→~6-7), F-172 (CORDIC Spin2 multi-return sigs, pnut-ts -d clean), F-173 (4-pin VIO/GIO power domain). Findability sweep across spin2 object-model files. |
| v1.12.0 | 2026-06-29 | **Add-on board hardware coverage (addon-wave-2026-06).** Authored 7 `hardware/` board YAMLs: microSD #64009, RTC #64013, HD-Audio #64014, WX-WiFi #32420, WX-Adapter #64007, Motor-Driver #64010, Prop-Plug #32201 (re-ingested first). Findability (F-116): aliases on all + `p2kb-categories.json` (eval_addon_boards +5, new connectivity + programming_tools) + compatibility-matrix rows. Drained datasheet/HW gaps from NO-COMMIT staged sources (gitignored): G-013 RTC PCF8523 `$68`+regmap, G-010 WX baud/command-protocol, G-011/Q-004 WX P2-path-via-#64007, G-014/Q-006 AK5704 `$10`+16-step DAC. Motor 9/8 silk-corrected (source OCR). |
| v1.11.2 | 2026-06-25 | **CORDIC rotation, COGINIT, and LSTRING accuracy.** QROTATE operand mapping corrected (X from D, Y from SETQ, angle from S, results via GETQX/GETQY) data-set-wide — `architecture/cordic.yaml` `rotate` + `rotation_matrix` and the `pasm2/pi.yaml` examples (F-166). COGINIT load size 496→504 longs ($000..$1F7) (F-167). LSTRING `{Spin2_v43}` version gate documented, pnut-ts-proven (F-168). Drains the DeSilva-tutorial release-gate. |
| v1.11.1 | 2026-06-25 | PASM2 reference accuracy: GETBRK per-flag-effect reference, the program counter (CALLD/CALL return address), and signed ADD/SUB C-flag semantics. |
| v1.11.0 | 2026-06-24 | **Eval-header board model + Assembly gate-drain.** Standardized all 10 eval-header boards to one self-contained shape (`pin_group.size` 8/16 + offset→signal `signal_map` + direction + `actual = base + offset`); authored the HyperRAM/HyperFlash board (#64004-ES); folded HUB75 onto the shape; removed 4 fabricated orphan boards; one `eval_addon_boards` category. Added the RDLUT/WRLUT immediate-address contract (F-161); removed the unsourced `pin_efficiency` metric (F-160); fixed the goertzel ultrasonic-as-pinout fabrication (F-162). Drains the Assembly manual's YAML-HEAD gate. |
| v1.10.1 | 2026-06-20 | Smart Pin reference depth and language accuracy (internal-consistency batch F-141…F-158 + Silicon-backed smart-pin additions G-001/002/003). |
| v1.10.0 | 2026-06-18 | DEBUG feed idioms + smart-pin sequencing (universal Reset→Setup→Enable→Operate order). |
| v1.9.1 | 2026-06-14 | DEBUG display directive accuracy (CLOSE, SIZE, legacy debug.yaml). |
| v1.9.0 | 2026-06-13 | Smart-pin & DEBUG accuracy + hardware findability (aliases + categories). |

## Eval-header board model (the base + offset convention)

Every P2 Eval add-on board plugs into one of the Eval Board's **8-pin accessory
headers** (or, for 16-pin boards, two adjacent headers). A board never owns fixed
P2 pins — it defines its functions by **offset** within its pin group, and the
user chooses which header it occupies. So:

> **`actual_P2_pin = base_pin + offset`** — `base_pin` is the first pin of the
> chosen group (8-pin: 0, 8, 16, 24, 32, 40, 48, 56; 16-pin: 0, 16, 32, 48).

Each board YAML is self-contained and carries this directly: `eval_header_occupant:
true`, `pin_group.size` (8 or 16), and a `signal_map` of `{offset, signal,
direction, notes}`. **Direction** is the P2-side role (out/in/bidir), stated only
where the source documents it. The browse-time answer to *"what eval boards do we
know about?"* is the `eval_addon_boards` category (10 boards).

## Known-hardware inventory

### Eval-header occupants (`eval_addon_boards` category)

| Part # | Board | Pins | Status |
|--------|-------|------|--------|
| 64006A | Control (4 LEDs + 4 buttons) | 8 | active |
| 64006B | Serial Host (twin USB-A) | 8 | active |
| 64006C | LED Matrix (8×7 Charlieplex, 56 LEDs) | 8 | active |
| 64006D | Digital Video Out (HDMI-type TMDS) | 8 | active |
| 64006E | Mini Prototyping (8×12 grid) | 8 | active |
| 64006F | Serial Device (twin microUSB) | 8 | active |
| 64006G | Goertzel / Touch (compass + switch pads) | 8 | active |
| 64006H | A/V Breakout (VGA + RCA + audio) | 8 | active |
| 64032 | HUB75 Adapter (RGB LED panel) | 16 | retiring (limited stock) |
| 64004-ES | HyperRAM + HyperFlash (16 MB + 32 MB) | 16 | limited edition |

### Host boards, modules, carriers & reference

| File | Kind |
|------|------|
| p2-eval-board | Host eval board (64000) |
| edge-standard-module · edge-32mb-module | Edge CPU modules |
| edge-breadboard-carrier · edge-mini-breakout · edge-standard-breakout | Edge carriers/breakouts |
| hardware-compatibility-matrix · p2-hardware-feature-comparison · p2-hardware-selection-guide · p1_rom_font_character_set | Reference data |

> Inventory and categories are kept 1:1 with the served files — adding or removing
> a board updates the file, `engineering/tools/p2kb-categories.json`, and this
> table together, and the regenerated index is asserted to match.
