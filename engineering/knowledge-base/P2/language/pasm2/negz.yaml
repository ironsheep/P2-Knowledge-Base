instruction: NEGZ
syntax: NEGZ Dest, {#}Src        {WC|WZ|WCZ}
encoding:
- EEEE 0110110 CZI DDDDDDDDD SSSSSSSSS                    D            Sign of result       Result
  = 0               2
- EEEE 0110110 CZ0 DDDDDDDDD DDDDDDDDD                    D            Sign of result       Result
  = 0               2
timing:
  cycles: 2
  type: fixed
description: 'NEGZ negates Src (syntax 1) or Dest (syntax 2) if Z = 1 and stores the
  result in the Dest register. If Z = 0, the Src or Result: The Src or Dest value,
  possibly negated according to Z, is stored into Dest and optionally the C and Z
  flags are updated to the resulting sign and zero status.


  NEGZ negates Src (syntax 1) or Dest (syntax 2) if Z = 1 and stores the result in
  the Dest register. If Z = 0, the Src or


  Dest value is left as-is (not negated) and is stored into Dest. If the negation
  is performed, it flips the value''s sign;


  ex: 526 becomes -526, or -41 becomes 41.


  If the WC or WCZ effect is specified, the C flag is set (1) if the result is negative,
  or is cleared (0) if positive.


  If the WZ or WCZ effect is specified, the Z flag is set (1) if the result is zero,
  or is cleared (0) if it is non-zero.'
flags_affected:
  Z: Result = 0
parameters:
- Dest is a register to receive the Src or -Src value (syntax 1), or contains the
  value to negate (syntax 2) according to Z.
- Src is an optional register, 9-bit literal, or 32-bit augmented literal whose value
  (if Z=0) or negated value (if Z=1) is stored into Dest.
- WC, WZ, or WCZ are optional effects to update flags.
related:
- NEGNZ
- NEGC
- NEGNC
documentation_source: PASM2 Manual 2022/11/01 Pages 31-147
documentation_level: comprehensive
compiler_operand_format:
  name: operand_du
  pattern: D{,S/#}
  description: Destination register, optional Source register or immediate (unary/binary)
  valueType: 3
compiler_encoding:
  operandFormat: 3
enhancement_source: PNUT_TS_v2.0_complete_operand_integration_2025-09-13
last_updated: '2025-09-19'
brief_description: Negate Z
category: Math and Logic
result: The Src or Dest value, possibly negated according to Z, is stored into Dest
  and optionally the C and Z flags
syntax_variants:
- NEGZ Dest, {#}Src        {WC|WZ|WCZ}
- NEGZ Dest                {WC|WZ|WCZ}
- NEGZ negates Src (syntax 1) or Dest (syntax 2) if Z = 1 and stores the result in
  the Dest register. If Z = 0, the Src or
manual_extraction_date: '2025-01-19'
oneliner: Negate value according to Z
