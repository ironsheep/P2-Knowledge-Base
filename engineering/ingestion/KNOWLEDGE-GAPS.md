# Knowledge Gaps & Questions-for-Experts — Moving Ledger

> Backing doc #3 of the ingestion **quad** (README dashboard + `AUTHORITATIVE-SOURCES` +
> `DOCUMENT-LINEAGE` + this). Added per the breadth study (`INGESTION-PERSPECTIVES-STUDY.md`, perspectives
> **#8 gap-evolution ledger** + **#9 questions-for-experts**) — the single biggest gap the triad was missing.
> Unlike the other backing docs (static trust / lineage), this is a **moving worklist**: holes open as new
> sources arrive and close as later sources / the designer fill them. _2026-06-12._

> **Next gap ID: `G-029`** · **Next expert-question ID: `Q-010`**
>
> This register **owns the `G-` and `Q-` allocators.** No other register may allocate them —
> `P2KB-CORRECTION-FINDINGS.md` routes a finding here rather than assigning a `G-` itself.

## Why this is its own doc
The dashboard answers "how complete is each source." This answers the orthogonal question: **"what does the KB
still not know, and who can answer it?"** It's the boundary of what's verifiable from sources at all. The
dashboard rolls it up as a Tier-1 line (open-questions count + how many are routed to an expert).

---

## Part A — Gap-evolution ledger  ‹perspective #8›

Each row is a knowledge hole. Status **moves**: `OPEN` → `ANSWERED` (cite the source/edition that filled it) →
or `STILL-UNKNOWN` (no source covers it; escalate to Part B). Record the **edition** that closed it so a
later supersession can re-open it if it overturns the answer.

| # | Domain | The gap (question / missing fact) | Status | Filled by (source @ edition) | Opened | Closed |
|---|--------|-----------------------------------|--------|------------------------------|--------|--------|
| G-001 | Smart Pins / WRPIN | WRPIN **%AAAA/%BBBB input-selector** relative-pin sub-field is undocumented in our YAML. Add with **Silicon-Doc** values: bit3=invert; `x000`=this pin, `x001..x011`=+1..+3, `x100`=this pin's OUT, `x101`=−3, `x110`=−2, `x111`=−1. *(Titus rev5 had x101/x111 swapped — use Silicon-Doc, not Titus.)* | **ANSWERED 2026-08-24** | **Two further Parallax sources now carry the whole table, both agreeing with Silicon-Doc and both refuting Titus rev5 on the two swapped rows: `x101` = relative −3, `x111` = relative −1.** **P2 Datasheet @ 2022/11/01** p.23 — the D-operand format line (`sources/p2-datasheet/p2-datasheet-text.txt:1052`) + the *(A) PIN or (B) ADJ Input Selector* table (`:1061-1074`). **P2 Hardware Manual @ 2022/11/01** — the identical table (`sources/p2-hardware-manual/p2-hardware-manual-text.txt:793-806`, Table 16). **The row's own premise was stale:** `deliverables/ai/P2/language/pasm2/wrpin.yaml:37-56` already carries the full `input_selectors:` block with these values (cited to Silicon Doc), so the YAML add is done. Residual defect found while checking → **F-331** (same file, `d_operand_format.fields` mislabels `M`/`TT`/`SSSSS`). | smart-pins-titus rev5 (#21) | 2026-08-24 |
| G-002 | Smart Pins / DAC dither | Dithered-DAC update cadence: sysclk or sysclk/256? Real effective resolution behind the "16-bit" claim (reviewer says ~10–12b realistic). | PARTIAL | **Cadence answered — the question's either/or is a false dichotomy; each dither mode has its own rule.** **P2 Hardware Manual @ 2022/11/01**: %00010 *noise* dither feeds the 8-bit DAC "pseudo-randomly-dithered data **on every clock**", and X[15:0] is only the *Y-capture* sample period — "set X[15:0] to **one**" for immediate effect (`sources/p2-hardware-manual/p2-hardware-manual-text.txt:1080,1084`). %00011 *PWM* dither: "the sample period **must be a multiple of 256** (X[7:0]=0)" (`:1091`). So reviewer #25's guessed sysclk/256 is right for PWM dither and wrong for noise dither. _Still open:_ #0's formula (how the random consumes the extended data) and **#26's real effective resolution behind the "16-bit" claim** — the manual states the mechanism and one artifact figure ("a frequency of Fclock/256 … at -48dB", `:1094`) but **no ENOB**. Escalated → **Q-008**. **Re-tested 2026-08-26 against the DOCX re-extraction — LEFT OPEN, and here is what was read.** The new extract adds a *worked example* the PDF capture never carried: `dacmode = %10100_00000000_01_00011_0 '990-ohm DAC + pwm-dithered 16-bit DAC mode` with `wxpin ##clks,pins32 'set period for three pwm-dithered DAC cycles` (`sources/silicon-doc/silicon-doc-text.txt:3428,3434`), plus the mode table rows `00010 = DAC 16-bit dither, noise` / `00011 = DAC 16-bit dither, PWM` (`:3812-3813`) and the mechanism sentence *"randomly dithers the 8-bit DAC between adjacent levels, in order to achieve 16-bit DAC output, averaged over time"*. **So the cadence half is now partly sourced — the period is set via WXPIN — but no ENOB figure and no dither formula exist anywhere in the document.** The half that remains open is the half only measurement or the designer can close (→ Q-008). | smart-pins-titus rev5 (#0,#25,#26) | |
| G-003 | Smart Pins / counting modes | "time" vs "states" vs "periods" terminology (%10011–%10111); reciprocal/"whole-periods" behavior — X+ is a *minimum*, window snaps to next whole Fin cycle. | **ANSWERED 2026-08-24** | **Both halves stated outright by the P2 Hardware Manual @ 2022/11/01.** *Terminology* — a **period** is "A-input rise/edge to B-input rise/edge" (`sources/p2-hardware-manual/p2-hardware-manual-text.txt:1254`); **time** = clock cycles and **states** = "A-input trigger states", counted across those periods, "the first mode … an oversampling period measurement, while the second mode is a complementary **duty** measurement" (`:1245-1246`); for %10101–%10111 explicitly: "The first mode accumulates **time** within each period … The second mode accumulates A-input trigger **states** … The third mode counts the **periods**" (`:1262-1264`). *Reciprocal/whole-periods* — reviewer #2's reading is confirmed verbatim: "X[31:0] establishes the **minimum** number of clock cycles to track periods for … until X clock cycles elapse and then **any period in progress completes**" (`:1254,:1261`), with the frequency/duty technique stated at `:1265`. Corroborated at name level by the **P2 Datasheet @ 2022/11/01** mode table (`sources/p2-datasheet/p2-datasheet-text.txt:1343-1352`), which words the same modes in the reverse order ("for periods in X+ clocks, count time") — a wording difference, not a conflict. | smart-pins-titus rev5 (#2,#14) | 2026-08-24 |
| G-004 | Smart Pins / %01010 SMPS | PWM switch-mode-power-supply mode has **no code example** anywhere; Y-register update timing unstated. | PARTIAL | **Y-update timing answered, and it confirms reviewer #22's guess.** **P2 Hardware Manual @ 2022/11/01**: Y[15:0] "gets captured at each frame start and used for its duration" (`sources/p2-hardware-manual/p2-hardware-manual-text.txt:1156`); the capture instant is the same instant IN rises — "After 'A' reads low, **Y[15:0] is captured, IN is raised**, and the process repeats" (`:1157`); WYPIN's value is "internally captured at the start of every PWM frame" (`:1162`); at reset "Y[15:0] is captured" (`:1160`). The manual also **describes the SMPS topology in prose** — 'A' = voltage detector on a divider centre-tap via internal-DAC comparison, 'B' = over-current detector on a GND-side shunt that forces the output low for the rest of the cycle, pin drives the switcher FET gate (`:1163-1165`). _Still open:_ #20's ask — there is still **no code example and no drawn schematic** for %01010 in any ingested source. **Re-tested 2026-08-26 — the Y-update-timing half is now ANSWERED; the code-example half is LEFT OPEN.** The DOCX extraction carries text the PDF capture did not: *"Due to the nature of switch-mode power supplies, it may be appropriate to just set Y.[15..0] once and let it repeat indefinitely"* (`sources/silicon-doc/silicon-doc-text.txt:4084`) and *"The 'A' input is the voltage detector for the SMPS output. This could be an adjacent pin using the internal-DAC-comparison mode to observe the center tap of a voltage divider"* (`:4090`). That settles the Y-register update cadence this row called unstated. **Still open: there is no `%01010` code example in the document** — searched by mode number and by name (`SMPS`, `switch-mode`), which returns the feature-list line `:208`, the mode-table row `:3820`, and the narrative at `:4070-4090`, none of them code. | smart-pins-titus rev5 (#20,#22) | |
| G-005 | Smart Pins / %11011 USB | Scope of smart-pin USB support; documented sysclk floor (FS-USB > 80 MHz, LS-USB less). | PARTIAL | **Scope answered in full.** **P2 Hardware Manual @ 2022/11/01** §*USB Host/Device (%11011)* (`sources/p2-hardware-manual/p2-hardware-manual-text.txt:1487-1493`) states the whole hardware interface: even/odd pin pair = DM/DP configured with identical `%1_11011_0` (or `%0_11011_0` for a non-driving **sniffer**); WXPIN on the lower pin sets D[15] host/device, D[14] full/low-speed, D[13:0] baud; the upper (odd) pin is DP and raises IN when the output buffer empties; start-up order; RDPIN/RQPIN status and WYPIN packet send. Corroborated by the **P2 Datasheet @ 2022/11/01** feature bullet "USB full-speed and low-speed (via odd/even pin pairs)" (`sources/p2-datasheet/p2-datasheet-text.txt:127`). **A sysclk dependency IS now documented — but not the one this row names:** the baud field is a 16-bit fraction of sysclk "whose two MSBs must be 0, **necessitating that the baud rate be less than 1/4th of the system clock frequency**" (`:1489`), with a worked 12 MHz-at-80 MHz example. _Still open:_ reviewer #24's **FS-USB > 80 MHz** floor and the "support is minimal / needs SW help" claim are stated by no source — they remain **Q-003**. Evidence noted on **F-274**, which was blocked on having no citable sysclk dependency. | smart-pins-titus rev5 (#24) | |
| G-006 | Smart Pins / NCO | Is `Y=0` valid for NCO (%00110/00111) — continuous output or none? NCO quantization jitter (fractional-add → rare long periods). | OPEN | _**Re-tested 2026-08-24 against the repaired P2 Hardware Manual and P2 Datasheet — deliberately NOT closed.** The manual now documents the complete NCO register semantics (`sources/p2-hardware-manual/p2-hardware-manual-text.txt:1116-1131`: X[15:0] base period; X[31:16]→Z[31:16] on WXPIN for phase; Y[31:0] added into Z[31:0] each base period; output = Z[31] for %00110 / Z-overflow for %00111; IN on Z overflow) — but it states **no `Y=0` case** and **no jitter characterisation**, and the datasheet carries no NCO narrative at all (`grep -n -i nco` on the datasheet returns only the feature bullet and the mode table). The Y=0 outcome is *determined by* the stated mechanism; stating it here would be inference, which does not close a row._ **Re-tested 2026-08-26 — LEFT OPEN, deliberately.** Read the whole `%00110 = NCO frequency` section (`sources/silicon-doc/silicon-doc-text.txt:4000-4010`). The document states *"Y.[31..0] will be added into Z.[31..0] at each base period"* and never addresses `Y=0`. Concluding *"Y=0 means Z never advances, so no rollover and no output"* is an **inference from the addition semantics, not a sourced statement**, and this project does not put inference in the KB. The quantization-jitter half is likewise absent. Closable by bench test or by the designer. | smart-pins-titus rev5 (#8,#17) | |
| G-007 | Smart Pins / ADC | %11000/11001 ADC modes: relationship to STREAMERS; "digital filters in digital mode"; external SDM-ADC use (TI AMC1035/1303). | **ANSWERED 2026-08-24** | **All three legs stated by the P2 Hardware Manual @ 2022/11/01** (`sources/p2-hardware-manual/p2-hardware-manual-text.txt`). **(#7 streamer)** — the answer corrects the question's premise: the ADC→streamer path is **not** %11000/%11001, whose per-sample-period results come back via RDPIN/RQPIN. It is the **SCOPE data pipe** fed by *scope* mode **%11010** — "Samples from blocks of up to four pins can be grouped into a 32-bit data pipe **for recording by the streamer** or reading by the GETSCP instruction" (`:1438`), "so that the streamer can record up to four **time-aligned** 8-bit ADC samples per clock", enabled by SETSCP D[6] / D[5:2] (`:1461-1468`). Corroborated by the **P2 Datasheet @ 2022/11/01**: "Streamer usage (background transfers from Hub RAM → pins/DACs, or **from pins/ADCs → Hub RAM**)" (`sources/p2-datasheet/p2-datasheet-text.txt:748`). **(#12 external SDM-ADC)** — "In the externally-clocked mode, the A-input will be sampled on each B-input rise, **so that an external delta-sigma ADC may be employed**" (`:1272`); the TI part numbers are the reviewer's examples, not something a P2 source carries. **(#23 digital filters are digital)** — confirmed: SINC2/SINC3 are bit-domain digital accumulators — "summing the **input bit** into an accumulator on each clock which, in turn, is summed into another accumulator" (`:1302-1303`, with the 27-bit accumulator width and the 512 / 11,585-clock decimation limits at `:1297`) — and the bitstream sub-mode "is really just capturing the A-input **without regard to pin configuration**" (`:1435`). Table 26 (`:1275-1295`) gives the full mode × sample-period ENOB grid. | smart-pins-titus rev5 (#7,#12,#23) | 2026-08-24 |
| G-008 | Smart Pins / WRPIN | Per-pin **Pin-Electrical** choices + full WRPIN mode-register bit-field map belong in the docs (reviewer cites evanh's bit-layout doc). | **ANSWERED 2026-08-24** | **The complete map is now carried, in full, by two agreeing Parallax sources — the KB no longer needs a third-party bit-layout doc for it.** **P2 Datasheet @ 2022/11/01** pp.23-25 (`sources/p2-datasheet/p2-datasheet-text.txt:1052-1123`): the `%AAAA_BBBB_FFF_MMMMMMMMMMMMM_TT_SSSSS_0` layout, the (A)/(B) input selector, (F) logic/filter, the 24-row **(M) Pin Mode** table with its Resulting-Internal-Configuration columns, the **Pin Mode Legend**, and the (T) DIR/OUT control block. **P2 Hardware Manual @ 2022/11/01** carries the same as Tables 16-23 + 25 (`sources/p2-hardware-manual/p2-hardware-manual-text.txt:793-806`, `:828-839`, `:883-910`, `:1020-1058`). **The "per-pin Pin-Electrical choices" are the `HHH`/`LLL` drive ladder** — `000` Fast · `001` 1.5 kΩ · `010` 15 kΩ · `011` 150 kΩ · `100` 1 mA · `101` 100 µA · `110` 10 µA · `111` Float — datasheet p.24 legend and Hardware-Manual nested Table 21 (`:859-863`), **identical in both**. Defect found while checking → **F-331** (`pasm2/wrpin.yaml` mislabels `M`/`TT`/`SSSSS`). | smart-pins-titus rev5 (#19) | 2026-08-24 |
| G-009 | Hardware / 64004-ES | HyperRAM/HyperFlash datasheet **part numbers + URLs are OCR-transcribed** (corrupt PDF text layer forced OCR) → transcription risk: HyperRAM `IS66WVH16M8BLL-100B1LI` / `issi.com/WW/pdf/66-67WVH16M8ALL-BLL.pdf`; HyperFlash `IS26KL256S-DABL100` / `issi.com/WW/pdf/26KS-KL128S-256S-512S.pdf`. Confirm exact strings vs ISSI datasheets / the 64004-ES product page before they reach published YAML. | OPEN | _verify vs ISSI datasheets / Parallax 64004-ES product page._ **Re-tested 2026-08-24** against the three repaired 2026-08-24 captures: no second source for these strings exists in the corpus. a case-insensitive `grep -c` for *hyperram*, *hyperflash*, *IS66* and *IS26* returns **0** in both `p2-datasheet-text.txt` and `p2-hardware-manual-text.txt`; the repaired **#64000 Eval Board** capture mentions HyperRAM exactly once and only as an application ("P0–P15 and P32–P47 are trace-length matched for high-speed data experiments (such as HyperRAM)", `sources/p2-eval-board/p2-eval-board-text.txt:107`) — **no part number**. The OCR-transcription risk is undiminished. | hyperRam-n-hyperFlash ingestion (2026-06-22) | |
| G-010 | Hardware / WX Wi-Fi #32420 | The guide names a "Command serial communication mode" and "default serial settings" but never enumerates the **UART baud/format** or the **command set/protocol**. Firmware is "Remora" (Parallax build of open-source esp-link by Thorsten von Eicken). | **ANSWERED 2026-06-29** | **Default baud 115200** (valid 1200/4800/9600/19200/38400/57600/74880/115200/230400/460800/921600; async RS-232-like on DO/DI). **Transparent (default) + Command modes**; enter Command mode via web Settings, `POST /wx/setting?name=cmd-enable&value=1`, or a DI break (≥30 bit-times). **Command framing: begin=$FE, end=$0D**; text-or-token forms (e.g. CLOSE=$E8); network commands over HTTP/WebSocket/TCP. Resolved from the **32420 Firmware/API Guide v1.0** (2016-11-16) + firmware v1.4, staged `sources/parallax-wx-wifi/NO-COMMIT/` (NOT committed). Folded into `hardware/addon-wx-wifi.yaml`. | parallax-wx-wifi re-extraction (2026-06-27) | |
| G-011 | Hardware / WX wireless-programming → P2 | #32420 DIP pins DTR (IO12 "toggles MCU reset") + PS (IO4) describe a **P1-era Activity-Board WX** wireless-programming flow (2016 doc); how/whether it maps to **P2** wireless programming/debug is unspecified. Same RES/PGM/CTS/DI/DO signals appear on the #64007 P2-WX-Adapter. | **RESOLVED 2026-06-29** | The module's native loader (proploader; `baud-rate`/`loader-baud-rate`) targets a P1 Propeller image. **P2 wireless programming = the SIP module on the #64007 P2 WX Adapter + the P2_httpd_xxxx.ota firmware** (drag/drop P2 binary loader; reset via CTS) — documented in `hardware/addon-wx-adapter.yaml`. The functional P2 path is established; only Parallax's marketing label for the bare module is open (→ Q-004). | parallax-wx-wifi + p2-wx-adapter re-extraction (2026-06-27) | |
| G-012 | Hardware / add-on `+N` offset | Add-on guides use **relative `+N` accessory-header offsets**; the **absolute P2 IO** each `+N` maps to is board/header-dependent and not given in the add-on guide (surfaced on #64009 microSD; systemic across the 12-pin add-on family). | OPEN | _resolve vs P2 Edge/eval-board accessory-header base-pin map._ **Re-tested 2026-08-24 against the digit-repaired #64000 Eval Board capture — NOT closable, and the re-test made the gap worse, not better.** What the guide does give: the eight edge headers each carry the invariant **2 × GND, 8 × I/O, 1 × Vxxxx, and one of {5 V, unconnected (P24-P31), RES (P56-P63)}** and accept "the 2x6 pass-through headers on eight different accessory add-ons" (`sources/p2-eval-board/p2-eval-board-text.txt:31-32`, `:482-520`; per-header pinouts in `sources/p2-eval-board/complete-p2-eval-board-reference.md:200-240`). What it does **not** give: the token `+N` appears nowhere in the guide, and there is no statement binding an add-on's `+0` to a header position. **New adverse evidence:** "**Header orientation varies by position on the board** — the p.17 silkscreen shows the P0–P7 header as GND/GND · P0/P1 · P2/P3 · P4/P5 · P6/P7 · 5V/V0007, i.e. the same 12 pins with the GND end and the power end swapped" (`complete-p2-eval-board-reference.md:236-239`), so the mapping is not even uniform across headers on one board. _(The `P0-P7`→`V0007` / `P8-P15`→`V0815` map that the guide does carry is the **power-group** map — a different thing, and not this.)_ | p2-microSD-addon ingestion (2026-06-27) | |
| G-013 | Hardware / RTC #64013 | Board guide defers RTC programming to the NXP **PCF8523** datasheet: the **I2C slave address** and the **register map** (time/date, alarm, timer, offset) are not in the guide. Cite the datasheet — do NOT infer the address. | **ANSWERED 2026-06-29** | **I2C 7-bit addr = `$68` (`%1101000`); register map 00h–13h** (Control_1/2/3, Seconds–Years 03h–09h BCD, alarms 0Ah–0Dh, Offset 0Eh, Timer/CLKOUT 0Fh–13h); reset = `$58`→00h. Resolved from the **NXP PCF8523 datasheet** (Tables 5 & 46) **+ corroborated** by the official Parallax **64013_RTC_Driver.spin2** (M. Mulholland/JonnyMac, MIT; `$68` @ 400 kHz, 3.3K pull-up). Both staged under `sources/P2-RTC-Add-on/NO-COMMIT/` (datasheet + example archive — NOT committed). Folded into `hardware/addon-rtc.yaml`. | P2-RTC-Add-on ingestion (2026-06-27) | |
| G-014 | Hardware / HD-Audio #64014 (AK5704) | The #64014 ADC board's on-board **AK5704 I2C device address** (set by the CAD strap pin) is not stated — needed to write a P2 I2C codec driver. The guide also lists only **4 of 16** DAC drive-impedance steps (full table absent). | **ANSWERED 2026-06-29** | **AK5704 7-bit addr = `$10`** (8-bit write `$20`/read `$21`; `AK5704_DEVICE_ID=$20`) @ 400 kHz; registers `$00–$46`. **The DAC '16 steps' = 4 P2 Smart-Pin DAC base modes** (`P_DAC_990R_3V`/`600R_2V`/`124R_3V`/`75R_2V`) **× 1–4 paralleled pins** (effective Z = base / pin-count). Resolved from the official Parallax **AK5704_Driver.spin2** + **4DAC_Analog_Driver.spin2** (MIT) + AK5704 datasheet + 64014-RevA schematic, staged `sources/P2-HD-Audio-Add-on/` (datasheet) & `NO-COMMIT/` (drivers+schematic, NOT committed). Folded into `hardware/addon-hd-audio.yaml`. | P2-HD-Audio-Add-on ingestion (2026-06-27) | |
| G-015 | Hardware / add-on example code | Every add-on guide in this wave (microSD/RTC/HD-Audio/motor-driver/WX) **defers example & driver code to the product-page download** — no in-repo driver/init code for these boards (AK5704 register-init, PCF8523 I2C, SD-SPI boot, motor PWM). Candidate future ingestion of the product-page example archives. | PARTIAL | _Example archives obtained 2026-06-29 (NO-COMMIT, MIT Spin2): **RTC #64013** (corroborated G-013), **HD-Audio #64014** (AK5704_Driver + 4DAC_Analog_Driver, corroborated G-014/Q-006), **WX #32420** (firmware v1.4 + API guide, corroborated G-010). All staged under their `sources/<src>/NO-COMMIT/`; NOT committed — candidates for a proper code-pass ingestion. microSD/motor-driver archives still outstanding._ **Re-tested 2026-08-24** against the three repaired captures: none carries add-on driver or example code. The #64000 Eval Board guide has no code at all; the P2 Hardware Manual's 51 code blocks are smart-pin/cog PASM2 fragments, not board drivers; the P2 Datasheet's code content is a 4-line HUBSET/WAITX sequence plus terminal transcripts (`sources/p2-datasheet/p2-datasheet-complete-extraction-audit.md:306-310`). The microSD/motor-driver archives remain outstanding. | addon-wave-2026-06 (2026-06-27) | |
| G-016 | Docs / P2 Hardware Manual erratum | **`COGATN   #00001100` does not assemble** (*Cog Attention* section). Its own comment reads *"Get attention of cogs 2 and 3"*, which needs bits 2 and 3 = `%00001100` = 12; as printed, `#00001100` is decimal 1,100 and `pnut-ts v1.55.3` rejects it — *Constant must be from 0 to 511 (m130)*. **A `%` is missing.** Confirmed verbatim in `word/document.xml`, so this is the document's defect, not an extraction artifact. Does **not** appear in `deliverables/ai/P2/`, so nothing shipped is wrong. | **RELOCATED → E-001** | _source-document erratum; report upstream to Parallax. Nothing to fix in our tree._ | p2-hardware-manual DOCX re-ingestion (2026-08-24) | | _(moved to `SOURCE-ERRATA.md` 2026-08-25: P2 Hardware Manual — `COGATN` missing `%`. A source-document defect is not a knowledge gap — a gap says WE DO NOT KNOW, an erratum says WE KNOW AND THE DOCUMENT IS WRONG. Row kept here, not deleted, so the ID resolves.)_
| G-017 | Docs / P2 Hardware Manual erratum | **`ROLBYTE y,x` does not assemble** — 4 occurrences in the *SCOPE Data Pipe* section. `ROLBYTE` has only two legal forms: `ROLBYTE D,{#}S,#N` and the `ALTGB`-alias `ROLBYTE D` (P2 Instructions v35, rows 94/95; both probed against `pnut-ts v1.55.3`, and the 2-operand form errors *Expected ","*). Per the block's own comments (*"rotate pinN byte into y"*, reading the `RDPIN` lower byte) the intended form is **`ROLBYTE y,x,#0`**. Confirmed verbatim in `word/document.xml`. Does **not** appear in `deliverables/ai/P2/`. | **RELOCATED → E-002** | _source-document erratum; report upstream to Parallax. Nothing to fix in our tree._ | p2-hardware-manual DOCX re-ingestion (2026-08-24) | | _(moved to `SOURCE-ERRATA.md` 2026-08-25: P2 Hardware Manual — 2-operand `ROLBYTE`. A source-document defect is not a knowledge gap — a gap says WE DO NOT KNOW, an erratum says WE KNOW AND THE DOCUMENT IS WRONG. Row kept here, not deleted, so the ID resolves.)_
| G-018 | Hardware / clock input ranges | The **P2 Hardware Manual** and the **P2 Datasheet** frame the external-clock limits differently and the KB must not silently pick one. Manual *Specifications*: *"10 - 20 MHz crystal (P2 Clock PLL enabled) or 0 to 180 MHz (nominal) clock oscillator"*. Datasheet *AC Characteristics* (p.48): Crystal (XI–XO) **1 min / 50 max MHz**; Direct drive (into XI) **DC min / 200 max MHz**; PLL **3.33 min / 320 max MHz**. These are **recommended-use vs absolute-limit** statements, not a contradiction — but a KB entry that quotes one as "the range" will be wrong for the other use. Decide which the KB states, and label it. | **RELOCATED → E-007** | _both sources agree on **180 MHz @ 105 °C nominal system clock** (verbatim in both), so only the input limits need the distinction._ **Both citations re-verified 2026-08-24 against the completed datasheet table artifact:** manual `sources/p2-hardware-manual/p2-hardware-manual-text.txt:203` (Table 2 *Specifications*) and datasheet `sources/p2-datasheet/complete-tables-reference.md:440-449` (p.48 AC Characteristics `Freq`, four extraction paths agreeing) — Crystal 1/50, Direct drive DC/200, PLL 3.33/320 MHz, footnote ² "Nominal PLL frequency (system clock speed) is 180 MHz at up to 105 °C". Still a **labelling decision owed to the KB**, not a source gap. | p2-hardware-manual DOCX re-ingestion (2026-08-24) | | _(moved to `SOURCE-ERRATA.md` 2026-08-25: clock-limit framing, recommended-use vs absolute-limit. A source-document defect is not a knowledge gap — a gap says WE DO NOT KNOW, an erratum says WE KNOW AND THE DOCUMENT IS WRONG. Row kept here, not deleted, so the ID resolves.)_
| G-019 | Electrical / I/O AC timing | **No P2 source states I/O-pin AC switching characteristics.** There is no propagation delay, no rise/fall time, no output-slew figure, and no per-drive-mode current in mA anywhere in the corpus. Measured, not assumed: the datasheet's **AC Characteristics table (p.48) has exactly two symbols — `Freq` and `Cin`** — confirmed by four extraction paths plus a visual read of the rendered page (`sources/p2-datasheet/complete-tables-reference.md:440-458`); the **entire 50-page datasheet contains one `ns` quantity**, p.3's "120-ohm (**3ns**)" DAC feature bullet, which does not say what it measures and is attached to a DAC, not to pin timing (`sources/p2-datasheet/p2-datasheet-text.txt:109`); the **Hardware Manual has no electrical-characteristics section at all** (`complete-tables-reference.md:496`) and **zero** `ns` quantities. The DC table's only current-vs-voltage data is `Vol`/`Voh` at 1/10/30 mA sink/source. **This is the hole F-329/F-327 fell into** — `architecture/io_pin_timing.yaml` carried a fabricated mA drive ladder and ns quantities that no extraction path supports. Closable only by Parallax publishing the numbers (→ **Q-007**) or by bench measurement. | OPEN | _source-silent across the whole corpus; do NOT let any ns/mA pin-timing number into the KB without a citation to something outside it_ **RE-TESTED 2026-08-26 against the completed silicon-doc DOCX extraction — CONFIRMED, with a refinement.** The verdict this row records was drawn while the Tier-1 authority was 75% ingested and contributing none of its 48 tables; it has now been re-run against the full extract. **The core stands: `propagation delay`, `rise time`, `fall time` and `slew rate` return ZERO hits.** All 9 `ns` quantities in the extract were read individually and none is a pin-driver AC spec — PLL jitter `<2ns @100us` (`silicon-doc-text.txt:81`), DAC settling `120-ohm (3ns)` (`:199`), the clock period `(at 6.25ns/clock)` (`:2807`), and the digital-filter table (`:2817`, `:2827`). **REFINEMENT — two I/O-electrical facts ARE now available that were not before, and this row should stop implying otherwise:** (1) **Table 30, the digital input-filter low-pass times**, computed in the document — `filt0: 6.25ns * 1 * 2 = 12.5ns`, `filt1: 6.25ns * 32 * 3 = 600ns` (`:2807-2830`); this lived inside a table cell, so no PDF-era capture could carry it. (2) the **drive-mode ladder** stated in one line at `:206`. Neither is a propagation/slew characterisation, so the gap is not closed — but "no per-drive-mode current in mA anywhere in the corpus" is now too strong and is corrected here. | p2-datasheet table recovery (2026-08-24) | |
| G-020 | Design guidance / thermal + layout | The datasheet **raises** power and thermal design as a first-class concern — "The Propeller 2 features notable power and thermal considerations in its pinout", the exposed under-side pad "serves … as a heatsink", "Connect to ground plane for thermal dissipation" (`sources/p2-datasheet/p2-datasheet-text.txt:269-276`, `:286-288`) — and then **answers none of it**. No thermal resistance (θJA/θJC) figure, no derating curve, no decoupling/PCB-layout recommendation, and no EMC guidance exists in any ingested source (a case-insensitive `grep -E` for *theta*, *thermal resist*, *junction-to-* and *derat* returns 0 hits in the datasheet and the Hardware Manual). What the corpus *does* bound: total power dissipation **2.5 W**, junction temp **+125 °C** max, operating **-40 to +105 °C**, 120 mA per VDD/Vxxyy pin, ±30 mA per I/O (`complete-tables-reference.md:393-412`). A designer cannot get from those to a thermal solution. | OPEN | _no source in the corpus covers it; a Parallax app-note or reference design would be the natural filler_ **RE-TESTED 2026-08-26 against the completed silicon-doc DOCX extraction — CONFIRMED.** Measured, not assumed: `thermal resist` 0 · `junction-to` 0 · `decoupl` 0 · `EMC` 0 · `heatsink` 0 · `heat sink` 0 · `ground plane` 0. Two apparent hits were checked and are false positives: the 4 `theta` matches are the CORDIC polar angle (`Rho32,Theta32`, `:179-181`), and the 3 `derat` matches are the substring inside `CONSIDERATIONS`/`considered`. The single `thermal` hit is *"31 bits of **thermal noise** gleaned from pin 63"* for PRNG seeding (`:2854`) — unrelated. **The Tier-1 authority, now fully extracted, contains no thermal-design content whatsoever.** | p2-datasheet table recovery (2026-08-24) | |
| G-021 | Package / mechanical | **The TQFP-100 package outline dimensions are not in text anywhere.** Datasheet **page 49 (PACKAGING) is an image-only page** — the extracted text for that page is the heading and the footer and nothing else (`sources/p2-datasheet/p2-datasheet-text.txt`, `=== PAGE 49 / 50 ===`); the drawing is captured as `page49_img01.png` but its dimensions were **not transcribed**, and the datasheet's own re-ingestion records this as owed (`p2-datasheet-complete-extraction-audit.md:304-312`). Unlike G-019/G-020 this is **closable from a source we already hold** — read the rendered page. | **ANSWERED 2026-08-26 — APPLIED** | _owed by our own extraction, not missing from the source; resolve by reading p.49 of the datasheet PDF_ **RE-TESTED 2026-08-26 — ✅ OVERTURNED. CLOSED.** This row said the TQFP-100 outline dimensions are "not in text anywhere" and that the datasheet's p.49 drawing was never transcribed. **The Silicon Doc carries its own copy of that drawing, and it is now extracted:** `assets/images-silicon-doc-2026-08-26/silicon-034.png` — the ON Semiconductor **MECHANICAL CASE OUTLINE / PACKAGE DIMENSIONS** sheet, **TQFP100 14x14, 0.5P, CASE 932BR, ISSUE O**, dated 03 JUL 2018, document **98AON94348G**. Full millimetre table, read off the rendered drawing **twice** (whole page, then the table at 3.5x zoom) with both reads agreeing, and **not** OCR'd — tesseract misreads digits on these sheets. **A**: max 1.20 · **A1**: 0.05/—/0.15 · **A2**: 0.95/1.00/1.05 · **b**: 0.17/0.22/0.27 · **c**: 0.20 REF · **D**: 15.80/16.00/16.20 · **D1**: 13.80/14.00/14.20 · **D2**: 9.50 REF · **E**: 15.80/16.00/16.20 · **E1**: 13.80/14.00/14.20 · **E2**: 9.50 REF · **e**: 0.50 BSC · **L**: 0.45/0.60/0.75 · **L1**: 1.00 REF · **M**: 0deg/—/7deg. Recommended mounting footprint also present: 16.83 / 9.60 outer, 100X 1.49, 100X 0.28, 0.50 pitch. **This row was correct that it was closable from a source we already hold — and the reason it stayed open is that the source had never been fully extracted.** Handing the transcription to the YAML head; this task does not edit `deliverables/`. **APPLIED to the shipped KB 2026-08-26 («#320»).** New record `deliverables/ai/P2/hardware/p2-package-mechanical.yaml` carries the full CASE 932BR dimension table (15 rows), the recommended mounting footprint, pin-corner numbering, and 12 aliases so it is reachable by name (`package dimensions`, `TQFP-100`, `footprint`, `land pattern`, …) — the KB had **no** package record of any kind before this. Every quantity carries a source. **The row's own disposition was right all along** — *closable from a source we already hold* — and what kept it open was that the source had never been fully extracted. ⚠️ **G-020 is NOT closed by this:** the drawing gives exposed-pad dimensions (D2/E2 9.50 REF) and **no** thermal resistance, derating curve or thermal-via guidance, and the new record says so explicitly so nobody infers a thermal solution from pad size. | p2-datasheet table recovery (2026-08-24) | |
| G-022 | Hardware / mikroBUS Click #64008 | **The MikroElektronika mikroBUS standard is not held in this repo, and it is the authority for Click-side signal MEANINGS.** The Parallax 64008 Rev A schematic settles the P2 side completely (all twelve offsets, read 2026-08-26) but gives UART net NAMES only -- `TX` -> offset +2, `RX` -> offset +3. Whether "TX" denotes the P2's transmit or the Click module's transmit is **not settled by any Parallax source we hold**. `architecture/click_module_integration.yaml` ships a crossover (P2 RX <- +2, P2 TX -> +3) whose only provenance is the P2-Click-eInk driver -- credible field evidence, but a peer derivation, not an authority. Also unsettled by the sheet: whether the `5V_B`/`VIO_3V3_B` rails on header B are commoned with the `_A` rails that actually feed the socket. | OPEN | _closable by ingesting the published mikroBUS specification from MikroElektronika (precedent exists: the NXP PCF8523 and AKM AK5704 datasheets closed G-013/G-014), or by bench continuity check for the rail question_ | p2-click-adapter ingestion (2026-08-26) | |
| G-023 | Smart Pins / %10xxx counting | **Does `IN` rise when the 'A' input reads LOW or HIGH** in the base-period counting mode? The Silicon Doc states low — *"the 'A' input is sampled at each base period until it reads low. After 'A' reads low, Y.[15..0] is captured, IN is raised"* (`sources/silicon-doc/silicon-doc-text.txt:4080`). Reviewer comment **[24]** (Anonymous, 2024-04-19), anchored to those very words, asserts *"I think IN is raised when smartA is high, not low."* No second documentary source covers this mode's trigger polarity, so the document cannot be checked against a peer. **Bench-testable and jumper-only** — drive a known waveform into A and observe IN. | OPEN | _the commenter is an unnamed reviewer, so this is an upstream lead, NOT a peer claim; do not flip the KB on it. Settle by hardware test (`hardware-verification/`) or by Chip Gracey._ | silicon-doc DOCX re-extraction (2026-08-26) | |
| G-024 | PASM2 / self-modifying code | **The two-instruction pipeline delay after SETS/SETD/SETR may hide a one-NOP hazard.** The Silicon Doc states *"It is necessary to elapse two instructions before executing the modified register, due to pipelining"* (`silicon-doc-text.txt`, SETS/SETD/SETR section). Reviewer comment **[12]** (Wuerfel21, 2025-05-20) anchored there says *"Maybe mention the nasty hazard that happens when there's only 1 NOP"* and cites a Parallax forum thread on a dual-port hazard. **The forum thread was NOT fetched** — this project does not probe forums.parallax.com — so the hazard is recorded by description only. | OPEN | _community lead, unverified; a figure or mechanism from that thread must NOT reach reader prose until confirmed against silicon or the designer. Closable by bench test or Chip Gracey._ | silicon-doc DOCX re-extraction (2026-08-26) | |
| G-025 | PASM2 / BITC + ADDPINS | **Is `BITC D,S` broken for a bit index above 31 since ADDPINS was introduced?** Reviewer comment **[11]** (rayslogic, 2022-08-25), anchored to `BITC 0,bitindex 'write C to bit[bitindex[4:0]]`, reports that ozpropdev demonstrated a break when `bitindex > 31`, attributing it to the addition of `ADDPINS`. The document's own comment says the index is masked to `[4:0]`, which would make values above 31 wrap rather than fail. **Probeable against `pnut-ts` and on hardware**, and not yet done. | OPEN | _community lead; the doc's `[4:0]` masking and the report are not obviously compatible, which is exactly why it needs a probe rather than a judgement._ | silicon-doc DOCX re-extraction (2026-08-26) | |
| G-026 | Boot / microSD selection | **What pin condition selects microSD boot?** Established (→ **E-014**): the Silicon Doc's boot sequence omits microSD entirely, while the Hardware Manual's boot-source table (`sources/p2-hardware-manual/p2-hardware-manual-text.txt:272-274`) and the ROM booter listing (`sources/rom-booter/rom-booter-narrative.txt:23`) both treat microSD as a first-class boot source. **What is NOT established is the mechanism.** Reviewer comment **[25]** proposes a pull-DOWN on P60; the Silicon Doc describes a pull-**up** on P60 (SPI_CK) for a different outcome, and the Hardware Manual expresses selection as a three-pin (P61/P60/P59) table. **Closable from a source we already hold** — `rom-booter` is at 40% and its `.lst` listings are on disk. | **ANSWERED 2026-08-26** | **Closed from a source we already hold — the P2-EC Edge Module Rev D Product Guide v3.0**, §6 Mode Selection Switch Bank (`sources/edge-standard-module/edge-standard-module-text.txt:195-225`; the ruled table independently recovered by `camelot lattice` as `edge-page-6-table-1.csv`, silkscreen triangles intact). **The mechanism is P59, not P60.** Verbatim: *"△ and ▽ are both connected to the Propeller 2 I/O pin **P59**; one with a pull-up resistor to 3.3V, and the other with a pull-down resistor to GND. You may see these boot mode selection pins referred to in other documentation as **P59 up and P59 down**."* The 6-row table gives the settings outright: **SD-card boot is FLASH=OFF**, with ▽ (P59 down) ON selecting *SD only, no serial window* and all-OFF selecting *SD with a 60 s serial fallback* — rows 1 and 5 share all-OFF and are disambiguated by **card presence**, which the guide states itself. **This corrects the claim that raised the gap:** Silicon Doc reviewer comment [25] proposed *a pulldown at P60*; the pin is **P59**, and the selector is a DIP switch pair, not a single pull-down. E-014 (the Silicon Doc omits microSD boot entirely) stands unchanged. **Already in the KB:** `hardware/edge-standard-module.yaml boot_modes` carries the full table, the P59 note and the row-1-vs-5 disambiguation — it was simply never connected to this gap. | silicon-doc DOCX re-extraction (2026-08-26) | |
| G-027 | Smart Pins / %11011 USB | **Can a new WRPIN reconfigure a RUNNING USB smart pin without resetting it?** Two editions of Propeller 2 Documentation v35 disagree. The PDF-derived capture says yes outright — *"A new WRPIN can be done to effect such a change without resetting the smart pin"* (`sources/silicon-doc/p2-documentation.txt:8886`) — and the DOCX capture of the same v35 document **omits the sentence entirely** (`sources/silicon-doc/silicon-doc-text.txt:4579`), while stating the opposite general rule at `:3856`: a WRPIN while DIR is high remaps 126 bits of state under a running mode, *"unpredictable and quite certainly useless behavior"*, so modes are configured only while DIR is low. **Either USB is a real exception (the sniffer change flips output drive rather than changing mode, so the multiplexing hazard may not apply) or the sentence was removed because it was wrong.** No document separates the two — they are the same document. **Bench-testable and jumper-only:** configure a USB pair, issue a new WRPIN with `%0_11011_0` while DIR is high, and observe whether the pin keeps working or breaks. | OPEN | _our KB currently states the PDF version as settled fact (→ **F-372**); the claim now carries the conflict rather than being deleted, since it may well be right_ | silicon-doc citation re-anchor (2026-08-26) | |
| G-028 | PASM2 / hardware call stack | **What does the 8-level hardware call stack do on the 9th push?** Every ingested source states the depth and nothing states the behaviour past it. Silicon Doc: *"8-level hardware stack for fastest subroutine calls/returns and push/pop operations"* (`sources/silicon-doc/silicon-doc-text.txt:166`) — feature-list only; the same sentence in the Datasheet (`sources/p2-datasheet/p2-datasheet-text.txt:75`). PASM2 Manual defines it as *"K / Stack: The 8-level hardware stack used for subroutine calls (stores C, Z, and PC) and temporary 32-bit push/pop storage"* (`sources/pasm2-manual/pasm2-manual-text.txt:776`) and describes CALL/RET as push/pop with no depth clause (`:1636`, `:1647`). **No source says whether a 9th push discards the oldest entry, wraps, or silently overwrites** — and one Silicon Doc passage points the other way for the bottom of the stack: in XBYTE, *"the bottom stack value gets copied each `_RET_` / RET"* (`:1150`, mechanism at `:897`), so under-run is defined and over-run is not. **This is the PASM-side twin of the Spin2 stack question in F-392, and it matters for the same reason:** a driver that nests CALLs 9 deep behind an interrupt corrupts its own return path with no diagnostic. **Bench-testable and jumper-only:** push 9 known values, pop 8, compare against what went in. | OPEN | _asked while researching stack-related instability; no ingested source covers it_ | debug/stack research (2026-08-30) | |


> **Format heritage (from the study):** three prior representations worth carrying — resolution-status tags +
> per-question source-check list (`questions-remaining.md`), strikethrough before/after (`gaps-consolidated.md`),
> and dated "what changed since" batches (`chip-clarifications-update`). This table unifies them.

## Part B — Questions for experts (the answerable-only-by-designer residue)  ‹perspective #9›

The subset of Part A that **no source can close** — only Chip Gracey (or another named authority) can. Carries a
**who-to-ask** routing so the question can actually be sent.

| # | Question | Why no source settles it | Who to ask | State (open / asked / answered) | Links |
|---|----------|--------------------------|------------|---------------------------------|-------|
| Q-001 | Is the sync-serial (%11100/%11101) description's "starts with a logic-0 start bit" correct, or does it wrongly borrow async framing? | Reviewer (#5) flagged it wrong, cited a now-unextractable Parallax-forum thread; no in-corpus source settles sync-serial framing detail. | Chip Gracey / Parallax forum (orig. thread) | open | smart-pins-titus #5 |
| Q-002 | Is the DAC dither frame fixed at 8 bits, or could a (e.g.) 4-bit extension give 12-bit DAC at a faster period? | Design-intent question about silicon capability not stated in any doc. **Factual half settled 2026-08-24:** the frame **is** fixed at 256 steps — the P2 Hardware Manual @ 2022/11/01 requires the %00011 PWM-dither sample period to be "a multiple of 256 (X[7:0]=0), so that an integral number of 256 steps are afforded the PWM" (`sources/p2-hardware-manual/p2-hardware-manual-text.txt:1091`). What remains is purely design-intent: whether a shorter frame *could* have been offered. | Chip Gracey | open | smart-pins-titus #3 |
| Q-003 | What is the realistic scope of smart-pin USB (%11011) support, and the SW/opcode help + sysclk needed for FS/LS-USB? | Implementation knowledge lives with the community implementer, not in docs. **Narrowed 2026-08-24:** the *hardware* scope is now fully documented (G-005), and the manual states one hard sysclk dependency — baud must be < sysclk/4 (`sources/p2-hardware-manual/p2-hardware-manual-text.txt:1489`). Still expert-only: whether FS-USB additionally needs sysclk **> 80 MHz** in practice, and how much SW/opcode help a working stack takes. | garryj (USB impl.) / Chip Gracey | open | smart-pins-titus #24 |
| Q-004 | Is the #32420 WX Wi-Fi Module officially supported for **Propeller 2** wireless programming/debug, or only the (P1-era) Propeller Activity Board WX the 2016 guide references? | The guide predates P2 boards; no in-corpus source settles P2 support. | Parallax | **largely resolved 2026-06-29** — functional P2 path IS documented (SIP module + #64007 P2 WX Adapter + P2_httpd firmware; see G-011). Remaining: confirm whether Parallax officially *labels* the bare #32420 as P2-supported. | parallax-wx-wifi (2026-06-27) |
| Q-005 | On the #64013 RTC board, +0 is shared between **I2C SCL** and **INT/CLKOUT**: is there a recommended sequence to switch +0 between the two roles without glitching an in-progress PCF8523 I2C transaction? | Board-design/timing intent not stated in the guide. | Chip Gracey / Parallax board author | open | P2-RTC-Add-on (2026-06-27) |
| Q-006 | Confirm the on-board **AK5704 CAD-strap → 7-bit I2C address** on the #64014 ADC board, and whether **SDTO2/TDMIN** on the header is wired data-out (SDTO2E=1) by default. | Set by board CAD strapping; only the schematic / Parallax can confirm. | Parallax (schematic) | **RESOLVED 2026-06-29** — CAD-strap → 7-bit `$10` (write `$20`); the official #64014 driver reads **SDTO2 (offset +5) as the Line-input data-OUT** of the codec by default. Confirmed via AK5704_Driver.spin2 + 64014-RevA schematic. | P2-HD-Audio-Add-on (2026-06-27) |
| Q-007 | Are the P2's **I/O-pin AC switching characteristics** (propagation delay, rise/fall time, per-drive-mode output current in mA) characterised at all — and if so, where are they published? | Not a wording gap: the datasheet's AC Characteristics table has exactly two symbols, `Freq` and `Cin`, verified by four extraction paths plus a visual page read; the Hardware Manual has no electrical section; the whole corpus holds **one** `ns` quantity and it belongs to a DAC. No documentary source can close it. | Parallax / Chip Gracey — else bench measurement (`hardware-verification/`) | open | G-019 · F-329 · F-327 |
| Q-008 | What is the **real effective resolution (ENOB)** of the %00010 / %00011 dithered "16-bit" DAC modes, and what is the formula by which the pseudo-random dither consumes the extended data? | The Hardware Manual states the *mechanism* (dither the 8-bit DAC between adjacent levels to reach 16-bit output averaged over time) and one artifact figure (Fclock/256 at -48 dB for PWM dither), but **no ENOB and no dither formula**. Reviewer Granville's "~10–12 bits realistic" is an upstream lead, not a citation. | Chip Gracey — else bench measurement | open | G-002 · smart-pins-titus #0, #26 |
| Q-009 | Under what conditions does **RDFAST corrupt**, and how many clock cycles must elapse before the FIFO is safe to use? | Established as real by the designer in the Silicon Doc's own review thread — Chip Gracey, replying to *"Should add the RDFAST corruption bug here"*, answers *"**Yes**, but I can't explain it well"* and later offers the mitigation *"just allow enough clock cycles before using the FIFO, like the instruction mode requires"* (`word/comments.xml` ids 0-4; → **E-015**). Both the designer and the reporter state outright they cannot explain the mechanism, so no document can close it. | Chip Gracey | open | E-015 · silicon-doc (2026-08-26) |

---

## Inputs that feed this ledger
- **`ingest-source` pass 6** (cross-source conflict audit) — unresolved conflicts and uncovered facts land here.
- **Reviewer notes harvested from source DOCX** — technical questions in embedded editorial notes / Google-Docs
  comments are routed here as credible feedback (e.g. Smart Pins (Titus) rev 5's 27 comments). See the project
  rule on ingesting reviewer notes.
- **The corrections register** — a finding that turns out to be unanswerable-from-sources is mirrored here as a
  Part-B question rather than left CONFIRMED-but-unfixable.

## Maintenance
Updated by `ingest-source` on every pass-6 and on each new edition (a supersession may ANSWER or RE-OPEN rows).
The dashboard's Tier-1 Q&A line reads its counts from here. Stale 2025 gap instances (`gaps-consolidated`,
`questions-remaining`, `AREAS-NOW-UNDERSTOOD`, …) fold into this ledger, then archive.
